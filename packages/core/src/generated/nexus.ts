// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v6.33.1
// source: nexus.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "./google/protobuf/timestamp";

export enum SessionStatus {
  UNKNOWN = 0,
  ACTIVE = 1,
  IDLE = 2,
  STALE = 3,
  ERRORED = 4,
  ENDED = 5,
  UNRECOGNIZED = -1,
}

export function sessionStatusFromJSON(object: any): SessionStatus {
  switch (object) {
    case 0:
    case "SESSION_STATUS_UNKNOWN":
      return SessionStatus.UNKNOWN;
    case 1:
    case "SESSION_STATUS_ACTIVE":
      return SessionStatus.ACTIVE;
    case 2:
    case "SESSION_STATUS_IDLE":
      return SessionStatus.IDLE;
    case 3:
    case "SESSION_STATUS_STALE":
      return SessionStatus.STALE;
    case 4:
    case "SESSION_STATUS_ERRORED":
      return SessionStatus.ERRORED;
    case 5:
    case "SESSION_STATUS_ENDED":
      return SessionStatus.ENDED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SessionStatus.UNRECOGNIZED;
  }
}

export function sessionStatusToJSON(object: SessionStatus): string {
  switch (object) {
    case SessionStatus.UNKNOWN:
      return "SESSION_STATUS_UNKNOWN";
    case SessionStatus.ACTIVE:
      return "SESSION_STATUS_ACTIVE";
    case SessionStatus.IDLE:
      return "SESSION_STATUS_IDLE";
    case SessionStatus.STALE:
      return "SESSION_STATUS_STALE";
    case SessionStatus.ERRORED:
      return "SESSION_STATUS_ERRORED";
    case SessionStatus.ENDED:
      return "SESSION_STATUS_ENDED";
    case SessionStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum SessionType {
  UNSPECIFIED = 0,
  MANAGED = 1,
  AD_HOC = 2,
  POOLED = 3,
  UNRECOGNIZED = -1,
}

export function sessionTypeFromJSON(object: any): SessionType {
  switch (object) {
    case 0:
    case "SESSION_TYPE_UNSPECIFIED":
      return SessionType.UNSPECIFIED;
    case 1:
    case "SESSION_TYPE_MANAGED":
      return SessionType.MANAGED;
    case 2:
    case "SESSION_TYPE_AD_HOC":
      return SessionType.AD_HOC;
    case 3:
    case "SESSION_TYPE_POOLED":
      return SessionType.POOLED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SessionType.UNRECOGNIZED;
  }
}

export function sessionTypeToJSON(object: SessionType): string {
  switch (object) {
    case SessionType.UNSPECIFIED:
      return "SESSION_TYPE_UNSPECIFIED";
    case SessionType.MANAGED:
      return "SESSION_TYPE_MANAGED";
    case SessionType.AD_HOC:
      return "SESSION_TYPE_AD_HOC";
    case SessionType.POOLED:
      return "SESSION_TYPE_POOLED";
    case SessionType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum EventType {
  UNSPECIFIED = 0,
  SESSION_STARTED = 1,
  HEARTBEAT_RECEIVED = 2,
  STATUS_CHANGED = 3,
  SESSION_STOPPED = 4,
  GOING_AWAY = 5,
  UNRECOGNIZED = -1,
}

export function eventTypeFromJSON(object: any): EventType {
  switch (object) {
    case 0:
    case "EVENT_TYPE_UNSPECIFIED":
      return EventType.UNSPECIFIED;
    case 1:
    case "EVENT_TYPE_SESSION_STARTED":
      return EventType.SESSION_STARTED;
    case 2:
    case "EVENT_TYPE_HEARTBEAT_RECEIVED":
      return EventType.HEARTBEAT_RECEIVED;
    case 3:
    case "EVENT_TYPE_STATUS_CHANGED":
      return EventType.STATUS_CHANGED;
    case 4:
    case "EVENT_TYPE_SESSION_STOPPED":
      return EventType.SESSION_STOPPED;
    case 5:
    case "EVENT_TYPE_GOING_AWAY":
      return EventType.GOING_AWAY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return EventType.UNRECOGNIZED;
  }
}

export function eventTypeToJSON(object: EventType): string {
  switch (object) {
    case EventType.UNSPECIFIED:
      return "EVENT_TYPE_UNSPECIFIED";
    case EventType.SESSION_STARTED:
      return "EVENT_TYPE_SESSION_STARTED";
    case EventType.HEARTBEAT_RECEIVED:
      return "EVENT_TYPE_HEARTBEAT_RECEIVED";
    case EventType.STATUS_CHANGED:
      return "EVENT_TYPE_STATUS_CHANGED";
    case EventType.SESSION_STOPPED:
      return "EVENT_TYPE_SESSION_STOPPED";
    case EventType.GOING_AWAY:
      return "EVENT_TYPE_GOING_AWAY";
    case EventType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum SyncStatus {
  UNSPECIFIED = 0,
  SYNCED = 1,
  BEHIND = 2,
  UNKNOWN = 3,
  UNRECOGNIZED = -1,
}

export function syncStatusFromJSON(object: any): SyncStatus {
  switch (object) {
    case 0:
    case "SYNC_STATUS_UNSPECIFIED":
      return SyncStatus.UNSPECIFIED;
    case 1:
    case "SYNCED":
      return SyncStatus.SYNCED;
    case 2:
    case "BEHIND":
      return SyncStatus.BEHIND;
    case 3:
    case "UNKNOWN":
      return SyncStatus.UNKNOWN;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SyncStatus.UNRECOGNIZED;
  }
}

export function syncStatusToJSON(object: SyncStatus): string {
  switch (object) {
    case SyncStatus.UNSPECIFIED:
      return "SYNC_STATUS_UNSPECIFIED";
    case SyncStatus.SYNCED:
      return "SYNCED";
    case SyncStatus.BEHIND:
      return "BEHIND";
    case SyncStatus.UNKNOWN:
      return "UNKNOWN";
    case SyncStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum CommandTier {
  UNSPECIFIED = 0,
  STATUS = 1,
  ANALYSIS = 2,
  ACTION = 3,
  UNRECOGNIZED = -1,
}

export function commandTierFromJSON(object: any): CommandTier {
  switch (object) {
    case 0:
    case "COMMAND_TIER_UNSPECIFIED":
      return CommandTier.UNSPECIFIED;
    case 1:
    case "COMMAND_TIER_STATUS":
      return CommandTier.STATUS;
    case 2:
    case "COMMAND_TIER_ANALYSIS":
      return CommandTier.ANALYSIS;
    case 3:
    case "COMMAND_TIER_ACTION":
      return CommandTier.ACTION;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CommandTier.UNRECOGNIZED;
  }
}

export function commandTierToJSON(object: CommandTier): string {
  switch (object) {
    case CommandTier.UNSPECIFIED:
      return "COMMAND_TIER_UNSPECIFIED";
    case CommandTier.STATUS:
      return "COMMAND_TIER_STATUS";
    case CommandTier.ANALYSIS:
      return "COMMAND_TIER_ANALYSIS";
    case CommandTier.ACTION:
      return "COMMAND_TIER_ACTION";
    case CommandTier.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum CostCategory {
  UNSPECIFIED = 0,
  MINIMAL = 1,
  LOW = 2,
  MEDIUM = 3,
  HIGH = 4,
  UNRECOGNIZED = -1,
}

export function costCategoryFromJSON(object: any): CostCategory {
  switch (object) {
    case 0:
    case "COST_CATEGORY_UNSPECIFIED":
      return CostCategory.UNSPECIFIED;
    case 1:
    case "COST_CATEGORY_MINIMAL":
      return CostCategory.MINIMAL;
    case 2:
    case "COST_CATEGORY_LOW":
      return CostCategory.LOW;
    case 3:
    case "COST_CATEGORY_MEDIUM":
      return CostCategory.MEDIUM;
    case 4:
    case "COST_CATEGORY_HIGH":
      return CostCategory.HIGH;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CostCategory.UNRECOGNIZED;
  }
}

export function costCategoryToJSON(object: CostCategory): string {
  switch (object) {
    case CostCategory.UNSPECIFIED:
      return "COST_CATEGORY_UNSPECIFIED";
    case CostCategory.MINIMAL:
      return "COST_CATEGORY_MINIMAL";
    case CostCategory.LOW:
      return "COST_CATEGORY_LOW";
    case CostCategory.MEDIUM:
      return "COST_CATEGORY_MEDIUM";
    case CostCategory.HIGH:
      return "COST_CATEGORY_HIGH";
    case CostCategory.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface RateLimitInfo {
  utilizationPercent: number;
  rateLimitType: string;
  surpassedThreshold: boolean;
}

export interface SessionTelemetry {
  rateLimit?: RateLimitInfo | undefined;
  totalCostUsd?: number | undefined;
  model?: string | undefined;
}

export interface Session {
  id: string;
  pid: number;
  project?: string | undefined;
  cwd: string;
  branch?: string | undefined;
  startedAt?: Date | undefined;
  lastHeartbeat?: Date | undefined;
  status: SessionStatus;
  sessionType: SessionType;
  spec?: string | undefined;
  command?: string | undefined;
  agent?: string | undefined;
  tmuxSession?: string | undefined;
  ccSessionId?: string | undefined;
  telemetry?: SessionTelemetry | undefined;
  tmuxTarget?: string | undefined;
  machine?: string | undefined;
  endedAt?: Date | undefined;
}

export interface SessionId {
  id: string;
}

export interface SessionFilter {
  status?: SessionStatus | undefined;
  project?: string | undefined;
  sessionType?: SessionType | undefined;
}

export interface SessionList {
  sessions: Session[];
  agentName: string;
  agentHost: string;
}

export interface StartSessionRequest {
  project: string;
  cwd: string;
  args: string[];
  targetAgent?: string | undefined;
}

export interface StartSessionResponse {
  sessionId: string;
  tmuxSession: string;
  sessionType: SessionType;
}

export interface StopResult {
  success: boolean;
  message?: string | undefined;
}

export interface RegisterSessionRequest {
  sessionId: string;
  pid: number;
  cwd: string;
  project?: string | undefined;
  branch?: string | undefined;
  command?: string | undefined;
}

export interface RegisterSessionResponse {
  sessionId: string;
  created: boolean;
}

export interface UnregisterSessionRequest {
  sessionId: string;
}

export interface UnregisterSessionResponse {
  found: boolean;
}

export interface HeartbeatRequest {
  sessionId: string;
}

export interface HeartbeatResponse {
  found: boolean;
}

export interface ProjectInfo {
  name: string;
  path?: string | undefined;
  gitBranch?: string | undefined;
  lastCommit?: string | undefined;
  syncStatus: SyncStatus;
  commitsBehind?: number | undefined;
}

export interface ListProjectsRequest {
}

export interface ListProjectsResponse {
  projects: string[];
  projectDetails: ProjectInfo[];
}

export interface AgentInfo {
  name: string;
  host: string;
  port: number;
}

export interface ListAgentsRequest {
}

export interface ListAgentsResponse {
  agents: AgentInfo[];
}

export interface CommandRequest {
  sessionId: string;
  prompt: string;
  project?: string | undefined;
}

export interface CommandOutput {
  sessionId: string;
  content?:
    | { $case: "text"; text: TextChunk }
    | { $case: "toolUse"; toolUse: ToolUseInfo }
    | { $case: "toolResult"; toolResult: ToolResult }
    | { $case: "error"; error: CommandError }
    | { $case: "done"; done: CommandDone }
    | { $case: "progress"; progress: ProgressUpdate }
    | undefined;
}

export interface TextChunk {
  text: string;
  partial: boolean;
}

export interface ToolUseInfo {
  toolName: string;
  inputPreview: string;
}

export interface ToolResult {
  toolName: string;
  outputPreview: string;
  success: boolean;
}

export interface CommandError {
  message: string;
  exitCode: number;
}

export interface CommandDone {
  durationMs: number;
  toolCalls: number;
}

export interface ProgressUpdate {
  phase: string;
  percent?: number | undefined;
  summary: string;
}

export interface EventFilter {
  sessionId?: string | undefined;
  eventTypes: EventType[];
  initialSnapshot: boolean;
}

export interface SessionEvent {
  sessionId: string;
  ts?: Date | undefined;
  payload?:
    | { $case: "started"; started: SessionStarted }
    | { $case: "heartbeat"; heartbeat: HeartbeatReceived }
    | { $case: "statusChanged"; statusChanged: StatusChanged }
    | { $case: "stopped"; stopped: SessionStopped }
    | { $case: "goingAway"; goingAway: GoingAway }
    | undefined;
  agentName: string;
}

export interface SessionStarted {
  session?: Session | undefined;
  isSnapshot: boolean;
}

export interface HeartbeatReceived {
  lastHeartbeat?: Date | undefined;
}

export interface StatusChanged {
  oldStatus: SessionStatus;
  newStatus: SessionStatus;
}

export interface SessionStopped {
  reason: string;
}

export interface GoingAway {
  reason: string;
  drainTimeoutMs: number;
}

export interface StatusTransition {
  fromStatus: SessionStatus;
  toStatus: SessionStatus;
  at?: Date | undefined;
  reason: string;
}

export interface ContainerStatus {
  name: string;
  running: boolean;
}

export interface CpuInfo {
  overallPercent: number;
  perCorePercent: number[];
  loadAverage: number[];
}

export interface RamInfo {
  totalBytes: number;
  usedBytes: number;
  percent: number;
}

export interface DiskInfo {
  mount: string;
  totalBytes: number;
  usedBytes: number;
  percent: number;
}

export interface DockerInfo {
  containers: number;
  running: number;
}

export interface NetworkInterface {
  iface: string;
  rxBytes: number;
  txBytes: number;
}

export interface ProcessEntry {
  pid: number;
  name: string;
  cpuPercent: number;
  ramPercent: number;
}

export interface ProcessSnapshot {
  topCpu: ProcessEntry[];
  topRam: ProcessEntry[];
}

export interface MachineHealth {
  hostname: string;
  uptimeSeconds: number;
  cpu?: CpuInfo | undefined;
  ram?: RamInfo | undefined;
  disk: DiskInfo[];
  docker?: DockerInfo | undefined;
  dockerContainers: ContainerStatus[];
  collectedAt?: Date | undefined;
  network: NetworkInterface[];
  processes?: ProcessSnapshot | undefined;
}

export interface HealthRequest {
}

export interface HealthResponse {
  agentName: string;
  agentHost: string;
  uptimeSeconds: number;
  sessionCount: number;
  machine?: MachineHealth | undefined;
  latestRateLimit?: RateLimitInfo | undefined;
}

export interface BeadsStatus {
  readyCount: number;
  openCount: number;
  blockedCount: number;
  readyJson: string;
}

export interface GitStatusInfo {
  branch: string;
  headSha: string;
  recentCommits: string[];
  porcelain: string;
}

export interface SpecStatusInfo {
  specCount: number;
  changeCount: number;
  activeChanges: string[];
}

export interface ProjectStatusRequest {
  project: string;
  fresh: boolean;
}

export interface ProjectStatusResponse {
  project: string;
  beads?: BeadsStatus | undefined;
  git?: GitStatusInfo | undefined;
  specs?: SpecStatusInfo | undefined;
}

export interface CommandInfoProto {
  name: string;
  namespace: string;
  fullName: string;
  description: string;
  tier: CommandTier;
  cost: CostCategory;
}

export interface ListCommandsRequest {
  namespace?: string | undefined;
  tier?: CommandTier | undefined;
}

export interface ListCommandsResponse {
  commands: CommandInfoProto[];
}

export interface RunProjectCommandRequest {
  project: string;
  command: string;
  args: string[];
  freshSession: boolean;
}

export interface SessionHistoryRequest {
  days: number;
  project?: string | undefined;
}

export interface SessionHistoryEntry {
  date: string;
  sessionCount: number;
  totalCostUsd: number;
}

export interface SessionHistoryResponse {
  entries: SessionHistoryEntry[];
}

export interface FailureTrendsRequest {
  days: number;
}

export interface FailureTrendEntry {
  date: string;
  count: number;
}

export interface FailureByTool {
  toolName: string;
  count: number;
}

export interface FailureTrendsResponse {
  daily: FailureTrendEntry[];
  byTool: FailureByTool[];
}

export interface HealthTimeSeriesRequest {
  hours: number;
}

export interface HealthTimeSeriesEntry {
  timestamp: string;
  cpuPercent: number;
  memoryUsedGb: number;
  diskUsedGb: number;
  load1: number;
}

export interface HealthTimeSeriesResponse {
  samples: HealthTimeSeriesEntry[];
}

export interface SpecVelocityRequest {
  days: number;
  project?: string | undefined;
}

export interface SpecVelocityEntry {
  date: string;
  project: string;
  specName: string;
  completedTasks: number;
  totalTasks: number;
}

export interface SpecVelocityResponse {
  entries: SpecVelocityEntry[];
}

function createBaseRateLimitInfo(): RateLimitInfo {
  return { utilizationPercent: 0, rateLimitType: "", surpassedThreshold: false };
}

export const RateLimitInfo: MessageFns<RateLimitInfo> = {
  encode(message: RateLimitInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.utilizationPercent !== 0) {
      writer.uint32(13).float(message.utilizationPercent);
    }
    if (message.rateLimitType !== "") {
      writer.uint32(18).string(message.rateLimitType);
    }
    if (message.surpassedThreshold !== false) {
      writer.uint32(24).bool(message.surpassedThreshold);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RateLimitInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRateLimitInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 13) {
            break;
          }

          message.utilizationPercent = reader.float();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.rateLimitType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.surpassedThreshold = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RateLimitInfo {
    return {
      utilizationPercent: isSet(object.utilizationPercent)
        ? globalThis.Number(object.utilizationPercent)
        : isSet(object.utilization_percent)
        ? globalThis.Number(object.utilization_percent)
        : 0,
      rateLimitType: isSet(object.rateLimitType)
        ? globalThis.String(object.rateLimitType)
        : isSet(object.rate_limit_type)
        ? globalThis.String(object.rate_limit_type)
        : "",
      surpassedThreshold: isSet(object.surpassedThreshold)
        ? globalThis.Boolean(object.surpassedThreshold)
        : isSet(object.surpassed_threshold)
        ? globalThis.Boolean(object.surpassed_threshold)
        : false,
    };
  },

  toJSON(message: RateLimitInfo): unknown {
    const obj: any = {};
    if (message.utilizationPercent !== 0) {
      obj.utilizationPercent = message.utilizationPercent;
    }
    if (message.rateLimitType !== "") {
      obj.rateLimitType = message.rateLimitType;
    }
    if (message.surpassedThreshold !== false) {
      obj.surpassedThreshold = message.surpassedThreshold;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RateLimitInfo>, I>>(base?: I): RateLimitInfo {
    return RateLimitInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RateLimitInfo>, I>>(object: I): RateLimitInfo {
    const message = createBaseRateLimitInfo();
    message.utilizationPercent = object.utilizationPercent ?? 0;
    message.rateLimitType = object.rateLimitType ?? "";
    message.surpassedThreshold = object.surpassedThreshold ?? false;
    return message;
  },
};

function createBaseSessionTelemetry(): SessionTelemetry {
  return { rateLimit: undefined, totalCostUsd: undefined, model: undefined };
}

export const SessionTelemetry: MessageFns<SessionTelemetry> = {
  encode(message: SessionTelemetry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rateLimit !== undefined) {
      RateLimitInfo.encode(message.rateLimit, writer.uint32(10).fork()).join();
    }
    if (message.totalCostUsd !== undefined) {
      writer.uint32(17).double(message.totalCostUsd);
    }
    if (message.model !== undefined) {
      writer.uint32(26).string(message.model);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SessionTelemetry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSessionTelemetry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rateLimit = RateLimitInfo.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.totalCostUsd = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.model = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SessionTelemetry {
    return {
      rateLimit: isSet(object.rateLimit)
        ? RateLimitInfo.fromJSON(object.rateLimit)
        : isSet(object.rate_limit)
        ? RateLimitInfo.fromJSON(object.rate_limit)
        : undefined,
      totalCostUsd: isSet(object.totalCostUsd)
        ? globalThis.Number(object.totalCostUsd)
        : isSet(object.total_cost_usd)
        ? globalThis.Number(object.total_cost_usd)
        : undefined,
      model: isSet(object.model) ? globalThis.String(object.model) : undefined,
    };
  },

  toJSON(message: SessionTelemetry): unknown {
    const obj: any = {};
    if (message.rateLimit !== undefined) {
      obj.rateLimit = RateLimitInfo.toJSON(message.rateLimit);
    }
    if (message.totalCostUsd !== undefined) {
      obj.totalCostUsd = message.totalCostUsd;
    }
    if (message.model !== undefined) {
      obj.model = message.model;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SessionTelemetry>, I>>(base?: I): SessionTelemetry {
    return SessionTelemetry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SessionTelemetry>, I>>(object: I): SessionTelemetry {
    const message = createBaseSessionTelemetry();
    message.rateLimit = (object.rateLimit !== undefined && object.rateLimit !== null)
      ? RateLimitInfo.fromPartial(object.rateLimit)
      : undefined;
    message.totalCostUsd = object.totalCostUsd ?? undefined;
    message.model = object.model ?? undefined;
    return message;
  },
};

function createBaseSession(): Session {
  return {
    id: "",
    pid: 0,
    project: undefined,
    cwd: "",
    branch: undefined,
    startedAt: undefined,
    lastHeartbeat: undefined,
    status: 0,
    sessionType: 0,
    spec: undefined,
    command: undefined,
    agent: undefined,
    tmuxSession: undefined,
    ccSessionId: undefined,
    telemetry: undefined,
    tmuxTarget: undefined,
    machine: undefined,
    endedAt: undefined,
  };
}

export const Session: MessageFns<Session> = {
  encode(message: Session, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.pid !== 0) {
      writer.uint32(16).uint32(message.pid);
    }
    if (message.project !== undefined) {
      writer.uint32(26).string(message.project);
    }
    if (message.cwd !== "") {
      writer.uint32(34).string(message.cwd);
    }
    if (message.branch !== undefined) {
      writer.uint32(42).string(message.branch);
    }
    if (message.startedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startedAt), writer.uint32(50).fork()).join();
    }
    if (message.lastHeartbeat !== undefined) {
      Timestamp.encode(toTimestamp(message.lastHeartbeat), writer.uint32(58).fork()).join();
    }
    if (message.status !== 0) {
      writer.uint32(64).int32(message.status);
    }
    if (message.sessionType !== 0) {
      writer.uint32(72).int32(message.sessionType);
    }
    if (message.spec !== undefined) {
      writer.uint32(82).string(message.spec);
    }
    if (message.command !== undefined) {
      writer.uint32(90).string(message.command);
    }
    if (message.agent !== undefined) {
      writer.uint32(98).string(message.agent);
    }
    if (message.tmuxSession !== undefined) {
      writer.uint32(106).string(message.tmuxSession);
    }
    if (message.ccSessionId !== undefined) {
      writer.uint32(114).string(message.ccSessionId);
    }
    if (message.telemetry !== undefined) {
      SessionTelemetry.encode(message.telemetry, writer.uint32(122).fork()).join();
    }
    if (message.tmuxTarget !== undefined) {
      writer.uint32(130).string(message.tmuxTarget);
    }
    if (message.machine !== undefined) {
      writer.uint32(138).string(message.machine);
    }
    if (message.endedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.endedAt), writer.uint32(146).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Session {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSession();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.pid = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.project = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.cwd = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.branch = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.startedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.lastHeartbeat = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.sessionType = reader.int32() as any;
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.spec = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.command = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.agent = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.tmuxSession = reader.string();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.ccSessionId = reader.string();
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.telemetry = SessionTelemetry.decode(reader, reader.uint32());
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.tmuxTarget = reader.string();
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.machine = reader.string();
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.endedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Session {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      pid: isSet(object.pid) ? globalThis.Number(object.pid) : 0,
      project: isSet(object.project) ? globalThis.String(object.project) : undefined,
      cwd: isSet(object.cwd) ? globalThis.String(object.cwd) : "",
      branch: isSet(object.branch) ? globalThis.String(object.branch) : undefined,
      startedAt: isSet(object.startedAt)
        ? fromJsonTimestamp(object.startedAt)
        : isSet(object.started_at)
        ? fromJsonTimestamp(object.started_at)
        : undefined,
      lastHeartbeat: isSet(object.lastHeartbeat)
        ? fromJsonTimestamp(object.lastHeartbeat)
        : isSet(object.last_heartbeat)
        ? fromJsonTimestamp(object.last_heartbeat)
        : undefined,
      status: isSet(object.status) ? sessionStatusFromJSON(object.status) : 0,
      sessionType: isSet(object.sessionType)
        ? sessionTypeFromJSON(object.sessionType)
        : isSet(object.session_type)
        ? sessionTypeFromJSON(object.session_type)
        : 0,
      spec: isSet(object.spec) ? globalThis.String(object.spec) : undefined,
      command: isSet(object.command) ? globalThis.String(object.command) : undefined,
      agent: isSet(object.agent) ? globalThis.String(object.agent) : undefined,
      tmuxSession: isSet(object.tmuxSession)
        ? globalThis.String(object.tmuxSession)
        : isSet(object.tmux_session)
        ? globalThis.String(object.tmux_session)
        : undefined,
      ccSessionId: isSet(object.ccSessionId)
        ? globalThis.String(object.ccSessionId)
        : isSet(object.cc_session_id)
        ? globalThis.String(object.cc_session_id)
        : undefined,
      telemetry: isSet(object.telemetry) ? SessionTelemetry.fromJSON(object.telemetry) : undefined,
      tmuxTarget: isSet(object.tmuxTarget)
        ? globalThis.String(object.tmuxTarget)
        : isSet(object.tmux_target)
        ? globalThis.String(object.tmux_target)
        : undefined,
      machine: isSet(object.machine) ? globalThis.String(object.machine) : undefined,
      endedAt: isSet(object.endedAt)
        ? fromJsonTimestamp(object.endedAt)
        : isSet(object.ended_at)
        ? fromJsonTimestamp(object.ended_at)
        : undefined,
    };
  },

  toJSON(message: Session): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.pid !== 0) {
      obj.pid = Math.round(message.pid);
    }
    if (message.project !== undefined) {
      obj.project = message.project;
    }
    if (message.cwd !== "") {
      obj.cwd = message.cwd;
    }
    if (message.branch !== undefined) {
      obj.branch = message.branch;
    }
    if (message.startedAt !== undefined) {
      obj.startedAt = message.startedAt.toISOString();
    }
    if (message.lastHeartbeat !== undefined) {
      obj.lastHeartbeat = message.lastHeartbeat.toISOString();
    }
    if (message.status !== 0) {
      obj.status = sessionStatusToJSON(message.status);
    }
    if (message.sessionType !== 0) {
      obj.sessionType = sessionTypeToJSON(message.sessionType);
    }
    if (message.spec !== undefined) {
      obj.spec = message.spec;
    }
    if (message.command !== undefined) {
      obj.command = message.command;
    }
    if (message.agent !== undefined) {
      obj.agent = message.agent;
    }
    if (message.tmuxSession !== undefined) {
      obj.tmuxSession = message.tmuxSession;
    }
    if (message.ccSessionId !== undefined) {
      obj.ccSessionId = message.ccSessionId;
    }
    if (message.telemetry !== undefined) {
      obj.telemetry = SessionTelemetry.toJSON(message.telemetry);
    }
    if (message.tmuxTarget !== undefined) {
      obj.tmuxTarget = message.tmuxTarget;
    }
    if (message.machine !== undefined) {
      obj.machine = message.machine;
    }
    if (message.endedAt !== undefined) {
      obj.endedAt = message.endedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Session>, I>>(base?: I): Session {
    return Session.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Session>, I>>(object: I): Session {
    const message = createBaseSession();
    message.id = object.id ?? "";
    message.pid = object.pid ?? 0;
    message.project = object.project ?? undefined;
    message.cwd = object.cwd ?? "";
    message.branch = object.branch ?? undefined;
    message.startedAt = object.startedAt ?? undefined;
    message.lastHeartbeat = object.lastHeartbeat ?? undefined;
    message.status = object.status ?? 0;
    message.sessionType = object.sessionType ?? 0;
    message.spec = object.spec ?? undefined;
    message.command = object.command ?? undefined;
    message.agent = object.agent ?? undefined;
    message.tmuxSession = object.tmuxSession ?? undefined;
    message.ccSessionId = object.ccSessionId ?? undefined;
    message.telemetry = (object.telemetry !== undefined && object.telemetry !== null)
      ? SessionTelemetry.fromPartial(object.telemetry)
      : undefined;
    message.tmuxTarget = object.tmuxTarget ?? undefined;
    message.machine = object.machine ?? undefined;
    message.endedAt = object.endedAt ?? undefined;
    return message;
  },
};

function createBaseSessionId(): SessionId {
  return { id: "" };
}

export const SessionId: MessageFns<SessionId> = {
  encode(message: SessionId, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SessionId {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSessionId();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SessionId {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: SessionId): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SessionId>, I>>(base?: I): SessionId {
    return SessionId.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SessionId>, I>>(object: I): SessionId {
    const message = createBaseSessionId();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseSessionFilter(): SessionFilter {
  return { status: undefined, project: undefined, sessionType: undefined };
}

export const SessionFilter: MessageFns<SessionFilter> = {
  encode(message: SessionFilter, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== undefined) {
      writer.uint32(8).int32(message.status);
    }
    if (message.project !== undefined) {
      writer.uint32(18).string(message.project);
    }
    if (message.sessionType !== undefined) {
      writer.uint32(24).int32(message.sessionType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SessionFilter {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSessionFilter();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.project = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sessionType = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SessionFilter {
    return {
      status: isSet(object.status) ? sessionStatusFromJSON(object.status) : undefined,
      project: isSet(object.project) ? globalThis.String(object.project) : undefined,
      sessionType: isSet(object.sessionType)
        ? sessionTypeFromJSON(object.sessionType)
        : isSet(object.session_type)
        ? sessionTypeFromJSON(object.session_type)
        : undefined,
    };
  },

  toJSON(message: SessionFilter): unknown {
    const obj: any = {};
    if (message.status !== undefined) {
      obj.status = sessionStatusToJSON(message.status);
    }
    if (message.project !== undefined) {
      obj.project = message.project;
    }
    if (message.sessionType !== undefined) {
      obj.sessionType = sessionTypeToJSON(message.sessionType);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SessionFilter>, I>>(base?: I): SessionFilter {
    return SessionFilter.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SessionFilter>, I>>(object: I): SessionFilter {
    const message = createBaseSessionFilter();
    message.status = object.status ?? undefined;
    message.project = object.project ?? undefined;
    message.sessionType = object.sessionType ?? undefined;
    return message;
  },
};

function createBaseSessionList(): SessionList {
  return { sessions: [], agentName: "", agentHost: "" };
}

export const SessionList: MessageFns<SessionList> = {
  encode(message: SessionList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.sessions) {
      Session.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.agentName !== "") {
      writer.uint32(18).string(message.agentName);
    }
    if (message.agentHost !== "") {
      writer.uint32(26).string(message.agentHost);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SessionList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSessionList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessions.push(Session.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.agentName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.agentHost = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SessionList {
    return {
      sessions: globalThis.Array.isArray(object?.sessions) ? object.sessions.map((e: any) => Session.fromJSON(e)) : [],
      agentName: isSet(object.agentName)
        ? globalThis.String(object.agentName)
        : isSet(object.agent_name)
        ? globalThis.String(object.agent_name)
        : "",
      agentHost: isSet(object.agentHost)
        ? globalThis.String(object.agentHost)
        : isSet(object.agent_host)
        ? globalThis.String(object.agent_host)
        : "",
    };
  },

  toJSON(message: SessionList): unknown {
    const obj: any = {};
    if (message.sessions?.length) {
      obj.sessions = message.sessions.map((e) => Session.toJSON(e));
    }
    if (message.agentName !== "") {
      obj.agentName = message.agentName;
    }
    if (message.agentHost !== "") {
      obj.agentHost = message.agentHost;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SessionList>, I>>(base?: I): SessionList {
    return SessionList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SessionList>, I>>(object: I): SessionList {
    const message = createBaseSessionList();
    message.sessions = object.sessions?.map((e) => Session.fromPartial(e)) || [];
    message.agentName = object.agentName ?? "";
    message.agentHost = object.agentHost ?? "";
    return message;
  },
};

function createBaseStartSessionRequest(): StartSessionRequest {
  return { project: "", cwd: "", args: [], targetAgent: undefined };
}

export const StartSessionRequest: MessageFns<StartSessionRequest> = {
  encode(message: StartSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.project !== "") {
      writer.uint32(10).string(message.project);
    }
    if (message.cwd !== "") {
      writer.uint32(18).string(message.cwd);
    }
    for (const v of message.args) {
      writer.uint32(26).string(v!);
    }
    if (message.targetAgent !== undefined) {
      writer.uint32(34).string(message.targetAgent);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartSessionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.project = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.cwd = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.args.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.targetAgent = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StartSessionRequest {
    return {
      project: isSet(object.project) ? globalThis.String(object.project) : "",
      cwd: isSet(object.cwd) ? globalThis.String(object.cwd) : "",
      args: globalThis.Array.isArray(object?.args) ? object.args.map((e: any) => globalThis.String(e)) : [],
      targetAgent: isSet(object.targetAgent)
        ? globalThis.String(object.targetAgent)
        : isSet(object.target_agent)
        ? globalThis.String(object.target_agent)
        : undefined,
    };
  },

  toJSON(message: StartSessionRequest): unknown {
    const obj: any = {};
    if (message.project !== "") {
      obj.project = message.project;
    }
    if (message.cwd !== "") {
      obj.cwd = message.cwd;
    }
    if (message.args?.length) {
      obj.args = message.args;
    }
    if (message.targetAgent !== undefined) {
      obj.targetAgent = message.targetAgent;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StartSessionRequest>, I>>(base?: I): StartSessionRequest {
    return StartSessionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartSessionRequest>, I>>(object: I): StartSessionRequest {
    const message = createBaseStartSessionRequest();
    message.project = object.project ?? "";
    message.cwd = object.cwd ?? "";
    message.args = object.args?.map((e) => e) || [];
    message.targetAgent = object.targetAgent ?? undefined;
    return message;
  },
};

function createBaseStartSessionResponse(): StartSessionResponse {
  return { sessionId: "", tmuxSession: "", sessionType: 0 };
}

export const StartSessionResponse: MessageFns<StartSessionResponse> = {
  encode(message: StartSessionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.tmuxSession !== "") {
      writer.uint32(18).string(message.tmuxSession);
    }
    if (message.sessionType !== 0) {
      writer.uint32(24).int32(message.sessionType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartSessionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartSessionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tmuxSession = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sessionType = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StartSessionResponse {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      tmuxSession: isSet(object.tmuxSession)
        ? globalThis.String(object.tmuxSession)
        : isSet(object.tmux_session)
        ? globalThis.String(object.tmux_session)
        : "",
      sessionType: isSet(object.sessionType)
        ? sessionTypeFromJSON(object.sessionType)
        : isSet(object.session_type)
        ? sessionTypeFromJSON(object.session_type)
        : 0,
    };
  },

  toJSON(message: StartSessionResponse): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.tmuxSession !== "") {
      obj.tmuxSession = message.tmuxSession;
    }
    if (message.sessionType !== 0) {
      obj.sessionType = sessionTypeToJSON(message.sessionType);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StartSessionResponse>, I>>(base?: I): StartSessionResponse {
    return StartSessionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartSessionResponse>, I>>(object: I): StartSessionResponse {
    const message = createBaseStartSessionResponse();
    message.sessionId = object.sessionId ?? "";
    message.tmuxSession = object.tmuxSession ?? "";
    message.sessionType = object.sessionType ?? 0;
    return message;
  },
};

function createBaseStopResult(): StopResult {
  return { success: false, message: undefined };
}

export const StopResult: MessageFns<StopResult> = {
  encode(message: StopResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== undefined) {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StopResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStopResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StopResult {
    return {
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : undefined,
    };
  },

  toJSON(message: StopResult): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== undefined) {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StopResult>, I>>(base?: I): StopResult {
    return StopResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StopResult>, I>>(object: I): StopResult {
    const message = createBaseStopResult();
    message.success = object.success ?? false;
    message.message = object.message ?? undefined;
    return message;
  },
};

function createBaseRegisterSessionRequest(): RegisterSessionRequest {
  return { sessionId: "", pid: 0, cwd: "", project: undefined, branch: undefined, command: undefined };
}

export const RegisterSessionRequest: MessageFns<RegisterSessionRequest> = {
  encode(message: RegisterSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.pid !== 0) {
      writer.uint32(16).uint32(message.pid);
    }
    if (message.cwd !== "") {
      writer.uint32(26).string(message.cwd);
    }
    if (message.project !== undefined) {
      writer.uint32(34).string(message.project);
    }
    if (message.branch !== undefined) {
      writer.uint32(42).string(message.branch);
    }
    if (message.command !== undefined) {
      writer.uint32(50).string(message.command);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterSessionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.pid = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.cwd = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.project = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.branch = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.command = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterSessionRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      pid: isSet(object.pid) ? globalThis.Number(object.pid) : 0,
      cwd: isSet(object.cwd) ? globalThis.String(object.cwd) : "",
      project: isSet(object.project) ? globalThis.String(object.project) : undefined,
      branch: isSet(object.branch) ? globalThis.String(object.branch) : undefined,
      command: isSet(object.command) ? globalThis.String(object.command) : undefined,
    };
  },

  toJSON(message: RegisterSessionRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.pid !== 0) {
      obj.pid = Math.round(message.pid);
    }
    if (message.cwd !== "") {
      obj.cwd = message.cwd;
    }
    if (message.project !== undefined) {
      obj.project = message.project;
    }
    if (message.branch !== undefined) {
      obj.branch = message.branch;
    }
    if (message.command !== undefined) {
      obj.command = message.command;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterSessionRequest>, I>>(base?: I): RegisterSessionRequest {
    return RegisterSessionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterSessionRequest>, I>>(object: I): RegisterSessionRequest {
    const message = createBaseRegisterSessionRequest();
    message.sessionId = object.sessionId ?? "";
    message.pid = object.pid ?? 0;
    message.cwd = object.cwd ?? "";
    message.project = object.project ?? undefined;
    message.branch = object.branch ?? undefined;
    message.command = object.command ?? undefined;
    return message;
  },
};

function createBaseRegisterSessionResponse(): RegisterSessionResponse {
  return { sessionId: "", created: false };
}

export const RegisterSessionResponse: MessageFns<RegisterSessionResponse> = {
  encode(message: RegisterSessionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.created !== false) {
      writer.uint32(16).bool(message.created);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterSessionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterSessionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.created = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterSessionResponse {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      created: isSet(object.created) ? globalThis.Boolean(object.created) : false,
    };
  },

  toJSON(message: RegisterSessionResponse): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.created !== false) {
      obj.created = message.created;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterSessionResponse>, I>>(base?: I): RegisterSessionResponse {
    return RegisterSessionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterSessionResponse>, I>>(object: I): RegisterSessionResponse {
    const message = createBaseRegisterSessionResponse();
    message.sessionId = object.sessionId ?? "";
    message.created = object.created ?? false;
    return message;
  },
};

function createBaseUnregisterSessionRequest(): UnregisterSessionRequest {
  return { sessionId: "" };
}

export const UnregisterSessionRequest: MessageFns<UnregisterSessionRequest> = {
  encode(message: UnregisterSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UnregisterSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnregisterSessionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnregisterSessionRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
    };
  },

  toJSON(message: UnregisterSessionRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UnregisterSessionRequest>, I>>(base?: I): UnregisterSessionRequest {
    return UnregisterSessionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnregisterSessionRequest>, I>>(object: I): UnregisterSessionRequest {
    const message = createBaseUnregisterSessionRequest();
    message.sessionId = object.sessionId ?? "";
    return message;
  },
};

function createBaseUnregisterSessionResponse(): UnregisterSessionResponse {
  return { found: false };
}

export const UnregisterSessionResponse: MessageFns<UnregisterSessionResponse> = {
  encode(message: UnregisterSessionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.found !== false) {
      writer.uint32(8).bool(message.found);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UnregisterSessionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUnregisterSessionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.found = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UnregisterSessionResponse {
    return { found: isSet(object.found) ? globalThis.Boolean(object.found) : false };
  },

  toJSON(message: UnregisterSessionResponse): unknown {
    const obj: any = {};
    if (message.found !== false) {
      obj.found = message.found;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UnregisterSessionResponse>, I>>(base?: I): UnregisterSessionResponse {
    return UnregisterSessionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UnregisterSessionResponse>, I>>(object: I): UnregisterSessionResponse {
    const message = createBaseUnregisterSessionResponse();
    message.found = object.found ?? false;
    return message;
  },
};

function createBaseHeartbeatRequest(): HeartbeatRequest {
  return { sessionId: "" };
}

export const HeartbeatRequest: MessageFns<HeartbeatRequest> = {
  encode(message: HeartbeatRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HeartbeatRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHeartbeatRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HeartbeatRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
    };
  },

  toJSON(message: HeartbeatRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HeartbeatRequest>, I>>(base?: I): HeartbeatRequest {
    return HeartbeatRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HeartbeatRequest>, I>>(object: I): HeartbeatRequest {
    const message = createBaseHeartbeatRequest();
    message.sessionId = object.sessionId ?? "";
    return message;
  },
};

function createBaseHeartbeatResponse(): HeartbeatResponse {
  return { found: false };
}

export const HeartbeatResponse: MessageFns<HeartbeatResponse> = {
  encode(message: HeartbeatResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.found !== false) {
      writer.uint32(8).bool(message.found);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HeartbeatResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHeartbeatResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.found = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HeartbeatResponse {
    return { found: isSet(object.found) ? globalThis.Boolean(object.found) : false };
  },

  toJSON(message: HeartbeatResponse): unknown {
    const obj: any = {};
    if (message.found !== false) {
      obj.found = message.found;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HeartbeatResponse>, I>>(base?: I): HeartbeatResponse {
    return HeartbeatResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HeartbeatResponse>, I>>(object: I): HeartbeatResponse {
    const message = createBaseHeartbeatResponse();
    message.found = object.found ?? false;
    return message;
  },
};

function createBaseProjectInfo(): ProjectInfo {
  return {
    name: "",
    path: undefined,
    gitBranch: undefined,
    lastCommit: undefined,
    syncStatus: 0,
    commitsBehind: undefined,
  };
}

export const ProjectInfo: MessageFns<ProjectInfo> = {
  encode(message: ProjectInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.path !== undefined) {
      writer.uint32(18).string(message.path);
    }
    if (message.gitBranch !== undefined) {
      writer.uint32(26).string(message.gitBranch);
    }
    if (message.lastCommit !== undefined) {
      writer.uint32(34).string(message.lastCommit);
    }
    if (message.syncStatus !== 0) {
      writer.uint32(40).int32(message.syncStatus);
    }
    if (message.commitsBehind !== undefined) {
      writer.uint32(48).int32(message.commitsBehind);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProjectInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProjectInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.gitBranch = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.lastCommit = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.syncStatus = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.commitsBehind = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProjectInfo {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      path: isSet(object.path) ? globalThis.String(object.path) : undefined,
      gitBranch: isSet(object.gitBranch)
        ? globalThis.String(object.gitBranch)
        : isSet(object.git_branch)
        ? globalThis.String(object.git_branch)
        : undefined,
      lastCommit: isSet(object.lastCommit)
        ? globalThis.String(object.lastCommit)
        : isSet(object.last_commit)
        ? globalThis.String(object.last_commit)
        : undefined,
      syncStatus: isSet(object.syncStatus)
        ? syncStatusFromJSON(object.syncStatus)
        : isSet(object.sync_status)
        ? syncStatusFromJSON(object.sync_status)
        : 0,
      commitsBehind: isSet(object.commitsBehind)
        ? globalThis.Number(object.commitsBehind)
        : isSet(object.commits_behind)
        ? globalThis.Number(object.commits_behind)
        : undefined,
    };
  },

  toJSON(message: ProjectInfo): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.path !== undefined) {
      obj.path = message.path;
    }
    if (message.gitBranch !== undefined) {
      obj.gitBranch = message.gitBranch;
    }
    if (message.lastCommit !== undefined) {
      obj.lastCommit = message.lastCommit;
    }
    if (message.syncStatus !== 0) {
      obj.syncStatus = syncStatusToJSON(message.syncStatus);
    }
    if (message.commitsBehind !== undefined) {
      obj.commitsBehind = Math.round(message.commitsBehind);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProjectInfo>, I>>(base?: I): ProjectInfo {
    return ProjectInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProjectInfo>, I>>(object: I): ProjectInfo {
    const message = createBaseProjectInfo();
    message.name = object.name ?? "";
    message.path = object.path ?? undefined;
    message.gitBranch = object.gitBranch ?? undefined;
    message.lastCommit = object.lastCommit ?? undefined;
    message.syncStatus = object.syncStatus ?? 0;
    message.commitsBehind = object.commitsBehind ?? undefined;
    return message;
  },
};

function createBaseListProjectsRequest(): ListProjectsRequest {
  return {};
}

export const ListProjectsRequest: MessageFns<ListProjectsRequest> = {
  encode(_: ListProjectsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListProjectsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListProjectsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListProjectsRequest {
    return {};
  },

  toJSON(_: ListProjectsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ListProjectsRequest>, I>>(base?: I): ListProjectsRequest {
    return ListProjectsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListProjectsRequest>, I>>(_: I): ListProjectsRequest {
    const message = createBaseListProjectsRequest();
    return message;
  },
};

function createBaseListProjectsResponse(): ListProjectsResponse {
  return { projects: [], projectDetails: [] };
}

export const ListProjectsResponse: MessageFns<ListProjectsResponse> = {
  encode(message: ListProjectsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.projects) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.projectDetails) {
      ProjectInfo.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListProjectsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListProjectsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.projects.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.projectDetails.push(ProjectInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListProjectsResponse {
    return {
      projects: globalThis.Array.isArray(object?.projects) ? object.projects.map((e: any) => globalThis.String(e)) : [],
      projectDetails: globalThis.Array.isArray(object?.projectDetails)
        ? object.projectDetails.map((e: any) => ProjectInfo.fromJSON(e))
        : globalThis.Array.isArray(object?.project_details)
        ? object.project_details.map((e: any) => ProjectInfo.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListProjectsResponse): unknown {
    const obj: any = {};
    if (message.projects?.length) {
      obj.projects = message.projects;
    }
    if (message.projectDetails?.length) {
      obj.projectDetails = message.projectDetails.map((e) => ProjectInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListProjectsResponse>, I>>(base?: I): ListProjectsResponse {
    return ListProjectsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListProjectsResponse>, I>>(object: I): ListProjectsResponse {
    const message = createBaseListProjectsResponse();
    message.projects = object.projects?.map((e) => e) || [];
    message.projectDetails = object.projectDetails?.map((e) => ProjectInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseAgentInfo(): AgentInfo {
  return { name: "", host: "", port: 0 };
}

export const AgentInfo: MessageFns<AgentInfo> = {
  encode(message: AgentInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.host !== "") {
      writer.uint32(18).string(message.host);
    }
    if (message.port !== 0) {
      writer.uint32(24).uint32(message.port);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AgentInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAgentInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.host = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.port = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AgentInfo {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      host: isSet(object.host) ? globalThis.String(object.host) : "",
      port: isSet(object.port) ? globalThis.Number(object.port) : 0,
    };
  },

  toJSON(message: AgentInfo): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.host !== "") {
      obj.host = message.host;
    }
    if (message.port !== 0) {
      obj.port = Math.round(message.port);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AgentInfo>, I>>(base?: I): AgentInfo {
    return AgentInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AgentInfo>, I>>(object: I): AgentInfo {
    const message = createBaseAgentInfo();
    message.name = object.name ?? "";
    message.host = object.host ?? "";
    message.port = object.port ?? 0;
    return message;
  },
};

function createBaseListAgentsRequest(): ListAgentsRequest {
  return {};
}

export const ListAgentsRequest: MessageFns<ListAgentsRequest> = {
  encode(_: ListAgentsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListAgentsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListAgentsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListAgentsRequest {
    return {};
  },

  toJSON(_: ListAgentsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ListAgentsRequest>, I>>(base?: I): ListAgentsRequest {
    return ListAgentsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListAgentsRequest>, I>>(_: I): ListAgentsRequest {
    const message = createBaseListAgentsRequest();
    return message;
  },
};

function createBaseListAgentsResponse(): ListAgentsResponse {
  return { agents: [] };
}

export const ListAgentsResponse: MessageFns<ListAgentsResponse> = {
  encode(message: ListAgentsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.agents) {
      AgentInfo.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListAgentsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListAgentsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.agents.push(AgentInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListAgentsResponse {
    return {
      agents: globalThis.Array.isArray(object?.agents) ? object.agents.map((e: any) => AgentInfo.fromJSON(e)) : [],
    };
  },

  toJSON(message: ListAgentsResponse): unknown {
    const obj: any = {};
    if (message.agents?.length) {
      obj.agents = message.agents.map((e) => AgentInfo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListAgentsResponse>, I>>(base?: I): ListAgentsResponse {
    return ListAgentsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListAgentsResponse>, I>>(object: I): ListAgentsResponse {
    const message = createBaseListAgentsResponse();
    message.agents = object.agents?.map((e) => AgentInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCommandRequest(): CommandRequest {
  return { sessionId: "", prompt: "", project: undefined };
}

export const CommandRequest: MessageFns<CommandRequest> = {
  encode(message: CommandRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.prompt !== "") {
      writer.uint32(18).string(message.prompt);
    }
    if (message.project !== undefined) {
      writer.uint32(26).string(message.project);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommandRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommandRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.prompt = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.project = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CommandRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      prompt: isSet(object.prompt) ? globalThis.String(object.prompt) : "",
      project: isSet(object.project) ? globalThis.String(object.project) : undefined,
    };
  },

  toJSON(message: CommandRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.prompt !== "") {
      obj.prompt = message.prompt;
    }
    if (message.project !== undefined) {
      obj.project = message.project;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CommandRequest>, I>>(base?: I): CommandRequest {
    return CommandRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CommandRequest>, I>>(object: I): CommandRequest {
    const message = createBaseCommandRequest();
    message.sessionId = object.sessionId ?? "";
    message.prompt = object.prompt ?? "";
    message.project = object.project ?? undefined;
    return message;
  },
};

function createBaseCommandOutput(): CommandOutput {
  return { sessionId: "", content: undefined };
}

export const CommandOutput: MessageFns<CommandOutput> = {
  encode(message: CommandOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    switch (message.content?.$case) {
      case "text":
        TextChunk.encode(message.content.text, writer.uint32(18).fork()).join();
        break;
      case "toolUse":
        ToolUseInfo.encode(message.content.toolUse, writer.uint32(26).fork()).join();
        break;
      case "toolResult":
        ToolResult.encode(message.content.toolResult, writer.uint32(34).fork()).join();
        break;
      case "error":
        CommandError.encode(message.content.error, writer.uint32(42).fork()).join();
        break;
      case "done":
        CommandDone.encode(message.content.done, writer.uint32(50).fork()).join();
        break;
      case "progress":
        ProgressUpdate.encode(message.content.progress, writer.uint32(58).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommandOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommandOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = { $case: "text", text: TextChunk.decode(reader, reader.uint32()) };
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.content = { $case: "toolUse", toolUse: ToolUseInfo.decode(reader, reader.uint32()) };
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.content = { $case: "toolResult", toolResult: ToolResult.decode(reader, reader.uint32()) };
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.content = { $case: "error", error: CommandError.decode(reader, reader.uint32()) };
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.content = { $case: "done", done: CommandDone.decode(reader, reader.uint32()) };
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.content = { $case: "progress", progress: ProgressUpdate.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CommandOutput {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      content: isSet(object.text)
        ? { $case: "text", text: TextChunk.fromJSON(object.text) }
        : isSet(object.toolUse)
        ? { $case: "toolUse", toolUse: ToolUseInfo.fromJSON(object.toolUse) }
        : isSet(object.tool_use)
        ? { $case: "toolUse", toolUse: ToolUseInfo.fromJSON(object.tool_use) }
        : isSet(object.toolResult)
        ? { $case: "toolResult", toolResult: ToolResult.fromJSON(object.toolResult) }
        : isSet(object.tool_result)
        ? { $case: "toolResult", toolResult: ToolResult.fromJSON(object.tool_result) }
        : isSet(object.error)
        ? { $case: "error", error: CommandError.fromJSON(object.error) }
        : isSet(object.done)
        ? { $case: "done", done: CommandDone.fromJSON(object.done) }
        : isSet(object.progress)
        ? { $case: "progress", progress: ProgressUpdate.fromJSON(object.progress) }
        : undefined,
    };
  },

  toJSON(message: CommandOutput): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.content?.$case === "text") {
      obj.text = TextChunk.toJSON(message.content.text);
    } else if (message.content?.$case === "toolUse") {
      obj.toolUse = ToolUseInfo.toJSON(message.content.toolUse);
    } else if (message.content?.$case === "toolResult") {
      obj.toolResult = ToolResult.toJSON(message.content.toolResult);
    } else if (message.content?.$case === "error") {
      obj.error = CommandError.toJSON(message.content.error);
    } else if (message.content?.$case === "done") {
      obj.done = CommandDone.toJSON(message.content.done);
    } else if (message.content?.$case === "progress") {
      obj.progress = ProgressUpdate.toJSON(message.content.progress);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CommandOutput>, I>>(base?: I): CommandOutput {
    return CommandOutput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CommandOutput>, I>>(object: I): CommandOutput {
    const message = createBaseCommandOutput();
    message.sessionId = object.sessionId ?? "";
    switch (object.content?.$case) {
      case "text": {
        if (object.content?.text !== undefined && object.content?.text !== null) {
          message.content = { $case: "text", text: TextChunk.fromPartial(object.content.text) };
        }
        break;
      }
      case "toolUse": {
        if (object.content?.toolUse !== undefined && object.content?.toolUse !== null) {
          message.content = { $case: "toolUse", toolUse: ToolUseInfo.fromPartial(object.content.toolUse) };
        }
        break;
      }
      case "toolResult": {
        if (object.content?.toolResult !== undefined && object.content?.toolResult !== null) {
          message.content = { $case: "toolResult", toolResult: ToolResult.fromPartial(object.content.toolResult) };
        }
        break;
      }
      case "error": {
        if (object.content?.error !== undefined && object.content?.error !== null) {
          message.content = { $case: "error", error: CommandError.fromPartial(object.content.error) };
        }
        break;
      }
      case "done": {
        if (object.content?.done !== undefined && object.content?.done !== null) {
          message.content = { $case: "done", done: CommandDone.fromPartial(object.content.done) };
        }
        break;
      }
      case "progress": {
        if (object.content?.progress !== undefined && object.content?.progress !== null) {
          message.content = { $case: "progress", progress: ProgressUpdate.fromPartial(object.content.progress) };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseTextChunk(): TextChunk {
  return { text: "", partial: false };
}

export const TextChunk: MessageFns<TextChunk> = {
  encode(message: TextChunk, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.partial !== false) {
      writer.uint32(16).bool(message.partial);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TextChunk {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTextChunk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.partial = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TextChunk {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      partial: isSet(object.partial) ? globalThis.Boolean(object.partial) : false,
    };
  },

  toJSON(message: TextChunk): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.partial !== false) {
      obj.partial = message.partial;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TextChunk>, I>>(base?: I): TextChunk {
    return TextChunk.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TextChunk>, I>>(object: I): TextChunk {
    const message = createBaseTextChunk();
    message.text = object.text ?? "";
    message.partial = object.partial ?? false;
    return message;
  },
};

function createBaseToolUseInfo(): ToolUseInfo {
  return { toolName: "", inputPreview: "" };
}

export const ToolUseInfo: MessageFns<ToolUseInfo> = {
  encode(message: ToolUseInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.toolName !== "") {
      writer.uint32(10).string(message.toolName);
    }
    if (message.inputPreview !== "") {
      writer.uint32(18).string(message.inputPreview);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolUseInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolUseInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.toolName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.inputPreview = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolUseInfo {
    return {
      toolName: isSet(object.toolName)
        ? globalThis.String(object.toolName)
        : isSet(object.tool_name)
        ? globalThis.String(object.tool_name)
        : "",
      inputPreview: isSet(object.inputPreview)
        ? globalThis.String(object.inputPreview)
        : isSet(object.input_preview)
        ? globalThis.String(object.input_preview)
        : "",
    };
  },

  toJSON(message: ToolUseInfo): unknown {
    const obj: any = {};
    if (message.toolName !== "") {
      obj.toolName = message.toolName;
    }
    if (message.inputPreview !== "") {
      obj.inputPreview = message.inputPreview;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolUseInfo>, I>>(base?: I): ToolUseInfo {
    return ToolUseInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolUseInfo>, I>>(object: I): ToolUseInfo {
    const message = createBaseToolUseInfo();
    message.toolName = object.toolName ?? "";
    message.inputPreview = object.inputPreview ?? "";
    return message;
  },
};

function createBaseToolResult(): ToolResult {
  return { toolName: "", outputPreview: "", success: false };
}

export const ToolResult: MessageFns<ToolResult> = {
  encode(message: ToolResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.toolName !== "") {
      writer.uint32(10).string(message.toolName);
    }
    if (message.outputPreview !== "") {
      writer.uint32(18).string(message.outputPreview);
    }
    if (message.success !== false) {
      writer.uint32(24).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ToolResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseToolResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.toolName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.outputPreview = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ToolResult {
    return {
      toolName: isSet(object.toolName)
        ? globalThis.String(object.toolName)
        : isSet(object.tool_name)
        ? globalThis.String(object.tool_name)
        : "",
      outputPreview: isSet(object.outputPreview)
        ? globalThis.String(object.outputPreview)
        : isSet(object.output_preview)
        ? globalThis.String(object.output_preview)
        : "",
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
    };
  },

  toJSON(message: ToolResult): unknown {
    const obj: any = {};
    if (message.toolName !== "") {
      obj.toolName = message.toolName;
    }
    if (message.outputPreview !== "") {
      obj.outputPreview = message.outputPreview;
    }
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ToolResult>, I>>(base?: I): ToolResult {
    return ToolResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ToolResult>, I>>(object: I): ToolResult {
    const message = createBaseToolResult();
    message.toolName = object.toolName ?? "";
    message.outputPreview = object.outputPreview ?? "";
    message.success = object.success ?? false;
    return message;
  },
};

function createBaseCommandError(): CommandError {
  return { message: "", exitCode: 0 };
}

export const CommandError: MessageFns<CommandError> = {
  encode(message: CommandError, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    if (message.exitCode !== 0) {
      writer.uint32(16).int32(message.exitCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommandError {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommandError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.exitCode = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CommandError {
    return {
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      exitCode: isSet(object.exitCode)
        ? globalThis.Number(object.exitCode)
        : isSet(object.exit_code)
        ? globalThis.Number(object.exit_code)
        : 0,
    };
  },

  toJSON(message: CommandError): unknown {
    const obj: any = {};
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.exitCode !== 0) {
      obj.exitCode = Math.round(message.exitCode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CommandError>, I>>(base?: I): CommandError {
    return CommandError.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CommandError>, I>>(object: I): CommandError {
    const message = createBaseCommandError();
    message.message = object.message ?? "";
    message.exitCode = object.exitCode ?? 0;
    return message;
  },
};

function createBaseCommandDone(): CommandDone {
  return { durationMs: 0, toolCalls: 0 };
}

export const CommandDone: MessageFns<CommandDone> = {
  encode(message: CommandDone, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.durationMs !== 0) {
      writer.uint32(8).uint64(message.durationMs);
    }
    if (message.toolCalls !== 0) {
      writer.uint32(16).uint32(message.toolCalls);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommandDone {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommandDone();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.durationMs = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.toolCalls = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CommandDone {
    return {
      durationMs: isSet(object.durationMs)
        ? globalThis.Number(object.durationMs)
        : isSet(object.duration_ms)
        ? globalThis.Number(object.duration_ms)
        : 0,
      toolCalls: isSet(object.toolCalls)
        ? globalThis.Number(object.toolCalls)
        : isSet(object.tool_calls)
        ? globalThis.Number(object.tool_calls)
        : 0,
    };
  },

  toJSON(message: CommandDone): unknown {
    const obj: any = {};
    if (message.durationMs !== 0) {
      obj.durationMs = Math.round(message.durationMs);
    }
    if (message.toolCalls !== 0) {
      obj.toolCalls = Math.round(message.toolCalls);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CommandDone>, I>>(base?: I): CommandDone {
    return CommandDone.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CommandDone>, I>>(object: I): CommandDone {
    const message = createBaseCommandDone();
    message.durationMs = object.durationMs ?? 0;
    message.toolCalls = object.toolCalls ?? 0;
    return message;
  },
};

function createBaseProgressUpdate(): ProgressUpdate {
  return { phase: "", percent: undefined, summary: "" };
}

export const ProgressUpdate: MessageFns<ProgressUpdate> = {
  encode(message: ProgressUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.phase !== "") {
      writer.uint32(10).string(message.phase);
    }
    if (message.percent !== undefined) {
      writer.uint32(21).float(message.percent);
    }
    if (message.summary !== "") {
      writer.uint32(26).string(message.summary);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProgressUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProgressUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.phase = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 21) {
            break;
          }

          message.percent = reader.float();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.summary = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProgressUpdate {
    return {
      phase: isSet(object.phase) ? globalThis.String(object.phase) : "",
      percent: isSet(object.percent) ? globalThis.Number(object.percent) : undefined,
      summary: isSet(object.summary) ? globalThis.String(object.summary) : "",
    };
  },

  toJSON(message: ProgressUpdate): unknown {
    const obj: any = {};
    if (message.phase !== "") {
      obj.phase = message.phase;
    }
    if (message.percent !== undefined) {
      obj.percent = message.percent;
    }
    if (message.summary !== "") {
      obj.summary = message.summary;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProgressUpdate>, I>>(base?: I): ProgressUpdate {
    return ProgressUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProgressUpdate>, I>>(object: I): ProgressUpdate {
    const message = createBaseProgressUpdate();
    message.phase = object.phase ?? "";
    message.percent = object.percent ?? undefined;
    message.summary = object.summary ?? "";
    return message;
  },
};

function createBaseEventFilter(): EventFilter {
  return { sessionId: undefined, eventTypes: [], initialSnapshot: false };
}

export const EventFilter: MessageFns<EventFilter> = {
  encode(message: EventFilter, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== undefined) {
      writer.uint32(10).string(message.sessionId);
    }
    writer.uint32(18).fork();
    for (const v of message.eventTypes) {
      writer.int32(v);
    }
    writer.join();
    if (message.initialSnapshot !== false) {
      writer.uint32(24).bool(message.initialSnapshot);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventFilter {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventFilter();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.eventTypes.push(reader.int32() as any);

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.eventTypes.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.initialSnapshot = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventFilter {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : undefined,
      eventTypes: globalThis.Array.isArray(object?.eventTypes)
        ? object.eventTypes.map((e: any) => eventTypeFromJSON(e))
        : globalThis.Array.isArray(object?.event_types)
        ? object.event_types.map((e: any) => eventTypeFromJSON(e))
        : [],
      initialSnapshot: isSet(object.initialSnapshot)
        ? globalThis.Boolean(object.initialSnapshot)
        : isSet(object.initial_snapshot)
        ? globalThis.Boolean(object.initial_snapshot)
        : false,
    };
  },

  toJSON(message: EventFilter): unknown {
    const obj: any = {};
    if (message.sessionId !== undefined) {
      obj.sessionId = message.sessionId;
    }
    if (message.eventTypes?.length) {
      obj.eventTypes = message.eventTypes.map((e) => eventTypeToJSON(e));
    }
    if (message.initialSnapshot !== false) {
      obj.initialSnapshot = message.initialSnapshot;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EventFilter>, I>>(base?: I): EventFilter {
    return EventFilter.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventFilter>, I>>(object: I): EventFilter {
    const message = createBaseEventFilter();
    message.sessionId = object.sessionId ?? undefined;
    message.eventTypes = object.eventTypes?.map((e) => e) || [];
    message.initialSnapshot = object.initialSnapshot ?? false;
    return message;
  },
};

function createBaseSessionEvent(): SessionEvent {
  return { sessionId: "", ts: undefined, payload: undefined, agentName: "" };
}

export const SessionEvent: MessageFns<SessionEvent> = {
  encode(message: SessionEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.ts !== undefined) {
      Timestamp.encode(toTimestamp(message.ts), writer.uint32(18).fork()).join();
    }
    switch (message.payload?.$case) {
      case "started":
        SessionStarted.encode(message.payload.started, writer.uint32(26).fork()).join();
        break;
      case "heartbeat":
        HeartbeatReceived.encode(message.payload.heartbeat, writer.uint32(34).fork()).join();
        break;
      case "statusChanged":
        StatusChanged.encode(message.payload.statusChanged, writer.uint32(42).fork()).join();
        break;
      case "stopped":
        SessionStopped.encode(message.payload.stopped, writer.uint32(50).fork()).join();
        break;
      case "goingAway":
        GoingAway.encode(message.payload.goingAway, writer.uint32(66).fork()).join();
        break;
    }
    if (message.agentName !== "") {
      writer.uint32(58).string(message.agentName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SessionEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSessionEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ts = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.payload = { $case: "started", started: SessionStarted.decode(reader, reader.uint32()) };
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.payload = { $case: "heartbeat", heartbeat: HeartbeatReceived.decode(reader, reader.uint32()) };
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.payload = { $case: "statusChanged", statusChanged: StatusChanged.decode(reader, reader.uint32()) };
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.payload = { $case: "stopped", stopped: SessionStopped.decode(reader, reader.uint32()) };
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.payload = { $case: "goingAway", goingAway: GoingAway.decode(reader, reader.uint32()) };
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.agentName = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SessionEvent {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      ts: isSet(object.ts) ? fromJsonTimestamp(object.ts) : undefined,
      payload: isSet(object.started)
        ? { $case: "started", started: SessionStarted.fromJSON(object.started) }
        : isSet(object.heartbeat)
        ? { $case: "heartbeat", heartbeat: HeartbeatReceived.fromJSON(object.heartbeat) }
        : isSet(object.statusChanged)
        ? { $case: "statusChanged", statusChanged: StatusChanged.fromJSON(object.statusChanged) }
        : isSet(object.status_changed)
        ? { $case: "statusChanged", statusChanged: StatusChanged.fromJSON(object.status_changed) }
        : isSet(object.stopped)
        ? { $case: "stopped", stopped: SessionStopped.fromJSON(object.stopped) }
        : isSet(object.goingAway)
        ? { $case: "goingAway", goingAway: GoingAway.fromJSON(object.goingAway) }
        : isSet(object.going_away)
        ? { $case: "goingAway", goingAway: GoingAway.fromJSON(object.going_away) }
        : undefined,
      agentName: isSet(object.agentName)
        ? globalThis.String(object.agentName)
        : isSet(object.agent_name)
        ? globalThis.String(object.agent_name)
        : "",
    };
  },

  toJSON(message: SessionEvent): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.ts !== undefined) {
      obj.ts = message.ts.toISOString();
    }
    if (message.payload?.$case === "started") {
      obj.started = SessionStarted.toJSON(message.payload.started);
    } else if (message.payload?.$case === "heartbeat") {
      obj.heartbeat = HeartbeatReceived.toJSON(message.payload.heartbeat);
    } else if (message.payload?.$case === "statusChanged") {
      obj.statusChanged = StatusChanged.toJSON(message.payload.statusChanged);
    } else if (message.payload?.$case === "stopped") {
      obj.stopped = SessionStopped.toJSON(message.payload.stopped);
    } else if (message.payload?.$case === "goingAway") {
      obj.goingAway = GoingAway.toJSON(message.payload.goingAway);
    }
    if (message.agentName !== "") {
      obj.agentName = message.agentName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SessionEvent>, I>>(base?: I): SessionEvent {
    return SessionEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SessionEvent>, I>>(object: I): SessionEvent {
    const message = createBaseSessionEvent();
    message.sessionId = object.sessionId ?? "";
    message.ts = object.ts ?? undefined;
    switch (object.payload?.$case) {
      case "started": {
        if (object.payload?.started !== undefined && object.payload?.started !== null) {
          message.payload = { $case: "started", started: SessionStarted.fromPartial(object.payload.started) };
        }
        break;
      }
      case "heartbeat": {
        if (object.payload?.heartbeat !== undefined && object.payload?.heartbeat !== null) {
          message.payload = { $case: "heartbeat", heartbeat: HeartbeatReceived.fromPartial(object.payload.heartbeat) };
        }
        break;
      }
      case "statusChanged": {
        if (object.payload?.statusChanged !== undefined && object.payload?.statusChanged !== null) {
          message.payload = {
            $case: "statusChanged",
            statusChanged: StatusChanged.fromPartial(object.payload.statusChanged),
          };
        }
        break;
      }
      case "stopped": {
        if (object.payload?.stopped !== undefined && object.payload?.stopped !== null) {
          message.payload = { $case: "stopped", stopped: SessionStopped.fromPartial(object.payload.stopped) };
        }
        break;
      }
      case "goingAway": {
        if (object.payload?.goingAway !== undefined && object.payload?.goingAway !== null) {
          message.payload = { $case: "goingAway", goingAway: GoingAway.fromPartial(object.payload.goingAway) };
        }
        break;
      }
    }
    message.agentName = object.agentName ?? "";
    return message;
  },
};

function createBaseSessionStarted(): SessionStarted {
  return { session: undefined, isSnapshot: false };
}

export const SessionStarted: MessageFns<SessionStarted> = {
  encode(message: SessionStarted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.session !== undefined) {
      Session.encode(message.session, writer.uint32(10).fork()).join();
    }
    if (message.isSnapshot !== false) {
      writer.uint32(16).bool(message.isSnapshot);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SessionStarted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSessionStarted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.session = Session.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isSnapshot = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SessionStarted {
    return {
      session: isSet(object.session) ? Session.fromJSON(object.session) : undefined,
      isSnapshot: isSet(object.isSnapshot)
        ? globalThis.Boolean(object.isSnapshot)
        : isSet(object.is_snapshot)
        ? globalThis.Boolean(object.is_snapshot)
        : false,
    };
  },

  toJSON(message: SessionStarted): unknown {
    const obj: any = {};
    if (message.session !== undefined) {
      obj.session = Session.toJSON(message.session);
    }
    if (message.isSnapshot !== false) {
      obj.isSnapshot = message.isSnapshot;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SessionStarted>, I>>(base?: I): SessionStarted {
    return SessionStarted.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SessionStarted>, I>>(object: I): SessionStarted {
    const message = createBaseSessionStarted();
    message.session = (object.session !== undefined && object.session !== null)
      ? Session.fromPartial(object.session)
      : undefined;
    message.isSnapshot = object.isSnapshot ?? false;
    return message;
  },
};

function createBaseHeartbeatReceived(): HeartbeatReceived {
  return { lastHeartbeat: undefined };
}

export const HeartbeatReceived: MessageFns<HeartbeatReceived> = {
  encode(message: HeartbeatReceived, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.lastHeartbeat !== undefined) {
      Timestamp.encode(toTimestamp(message.lastHeartbeat), writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HeartbeatReceived {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHeartbeatReceived();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.lastHeartbeat = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HeartbeatReceived {
    return {
      lastHeartbeat: isSet(object.lastHeartbeat)
        ? fromJsonTimestamp(object.lastHeartbeat)
        : isSet(object.last_heartbeat)
        ? fromJsonTimestamp(object.last_heartbeat)
        : undefined,
    };
  },

  toJSON(message: HeartbeatReceived): unknown {
    const obj: any = {};
    if (message.lastHeartbeat !== undefined) {
      obj.lastHeartbeat = message.lastHeartbeat.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HeartbeatReceived>, I>>(base?: I): HeartbeatReceived {
    return HeartbeatReceived.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HeartbeatReceived>, I>>(object: I): HeartbeatReceived {
    const message = createBaseHeartbeatReceived();
    message.lastHeartbeat = object.lastHeartbeat ?? undefined;
    return message;
  },
};

function createBaseStatusChanged(): StatusChanged {
  return { oldStatus: 0, newStatus: 0 };
}

export const StatusChanged: MessageFns<StatusChanged> = {
  encode(message: StatusChanged, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.oldStatus !== 0) {
      writer.uint32(8).int32(message.oldStatus);
    }
    if (message.newStatus !== 0) {
      writer.uint32(16).int32(message.newStatus);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StatusChanged {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatusChanged();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.oldStatus = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.newStatus = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StatusChanged {
    return {
      oldStatus: isSet(object.oldStatus)
        ? sessionStatusFromJSON(object.oldStatus)
        : isSet(object.old_status)
        ? sessionStatusFromJSON(object.old_status)
        : 0,
      newStatus: isSet(object.newStatus)
        ? sessionStatusFromJSON(object.newStatus)
        : isSet(object.new_status)
        ? sessionStatusFromJSON(object.new_status)
        : 0,
    };
  },

  toJSON(message: StatusChanged): unknown {
    const obj: any = {};
    if (message.oldStatus !== 0) {
      obj.oldStatus = sessionStatusToJSON(message.oldStatus);
    }
    if (message.newStatus !== 0) {
      obj.newStatus = sessionStatusToJSON(message.newStatus);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StatusChanged>, I>>(base?: I): StatusChanged {
    return StatusChanged.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StatusChanged>, I>>(object: I): StatusChanged {
    const message = createBaseStatusChanged();
    message.oldStatus = object.oldStatus ?? 0;
    message.newStatus = object.newStatus ?? 0;
    return message;
  },
};

function createBaseSessionStopped(): SessionStopped {
  return { reason: "" };
}

export const SessionStopped: MessageFns<SessionStopped> = {
  encode(message: SessionStopped, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reason !== "") {
      writer.uint32(10).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SessionStopped {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSessionStopped();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SessionStopped {
    return { reason: isSet(object.reason) ? globalThis.String(object.reason) : "" };
  },

  toJSON(message: SessionStopped): unknown {
    const obj: any = {};
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SessionStopped>, I>>(base?: I): SessionStopped {
    return SessionStopped.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SessionStopped>, I>>(object: I): SessionStopped {
    const message = createBaseSessionStopped();
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseGoingAway(): GoingAway {
  return { reason: "", drainTimeoutMs: 0 };
}

export const GoingAway: MessageFns<GoingAway> = {
  encode(message: GoingAway, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reason !== "") {
      writer.uint32(10).string(message.reason);
    }
    if (message.drainTimeoutMs !== 0) {
      writer.uint32(16).uint32(message.drainTimeoutMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GoingAway {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGoingAway();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.drainTimeoutMs = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GoingAway {
    return {
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      drainTimeoutMs: isSet(object.drainTimeoutMs)
        ? globalThis.Number(object.drainTimeoutMs)
        : isSet(object.drain_timeout_ms)
        ? globalThis.Number(object.drain_timeout_ms)
        : 0,
    };
  },

  toJSON(message: GoingAway): unknown {
    const obj: any = {};
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.drainTimeoutMs !== 0) {
      obj.drainTimeoutMs = Math.round(message.drainTimeoutMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GoingAway>, I>>(base?: I): GoingAway {
    return GoingAway.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GoingAway>, I>>(object: I): GoingAway {
    const message = createBaseGoingAway();
    message.reason = object.reason ?? "";
    message.drainTimeoutMs = object.drainTimeoutMs ?? 0;
    return message;
  },
};

function createBaseStatusTransition(): StatusTransition {
  return { fromStatus: 0, toStatus: 0, at: undefined, reason: "" };
}

export const StatusTransition: MessageFns<StatusTransition> = {
  encode(message: StatusTransition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.fromStatus !== 0) {
      writer.uint32(8).int32(message.fromStatus);
    }
    if (message.toStatus !== 0) {
      writer.uint32(16).int32(message.toStatus);
    }
    if (message.at !== undefined) {
      Timestamp.encode(toTimestamp(message.at), writer.uint32(26).fork()).join();
    }
    if (message.reason !== "") {
      writer.uint32(34).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StatusTransition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatusTransition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.fromStatus = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.toStatus = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.at = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StatusTransition {
    return {
      fromStatus: isSet(object.fromStatus)
        ? sessionStatusFromJSON(object.fromStatus)
        : isSet(object.from_status)
        ? sessionStatusFromJSON(object.from_status)
        : 0,
      toStatus: isSet(object.toStatus)
        ? sessionStatusFromJSON(object.toStatus)
        : isSet(object.to_status)
        ? sessionStatusFromJSON(object.to_status)
        : 0,
      at: isSet(object.at) ? fromJsonTimestamp(object.at) : undefined,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
    };
  },

  toJSON(message: StatusTransition): unknown {
    const obj: any = {};
    if (message.fromStatus !== 0) {
      obj.fromStatus = sessionStatusToJSON(message.fromStatus);
    }
    if (message.toStatus !== 0) {
      obj.toStatus = sessionStatusToJSON(message.toStatus);
    }
    if (message.at !== undefined) {
      obj.at = message.at.toISOString();
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StatusTransition>, I>>(base?: I): StatusTransition {
    return StatusTransition.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StatusTransition>, I>>(object: I): StatusTransition {
    const message = createBaseStatusTransition();
    message.fromStatus = object.fromStatus ?? 0;
    message.toStatus = object.toStatus ?? 0;
    message.at = object.at ?? undefined;
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseContainerStatus(): ContainerStatus {
  return { name: "", running: false };
}

export const ContainerStatus: MessageFns<ContainerStatus> = {
  encode(message: ContainerStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.running !== false) {
      writer.uint32(16).bool(message.running);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContainerStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContainerStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.running = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContainerStatus {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      running: isSet(object.running) ? globalThis.Boolean(object.running) : false,
    };
  },

  toJSON(message: ContainerStatus): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.running !== false) {
      obj.running = message.running;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ContainerStatus>, I>>(base?: I): ContainerStatus {
    return ContainerStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ContainerStatus>, I>>(object: I): ContainerStatus {
    const message = createBaseContainerStatus();
    message.name = object.name ?? "";
    message.running = object.running ?? false;
    return message;
  },
};

function createBaseCpuInfo(): CpuInfo {
  return { overallPercent: 0, perCorePercent: [], loadAverage: [] };
}

export const CpuInfo: MessageFns<CpuInfo> = {
  encode(message: CpuInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.overallPercent !== 0) {
      writer.uint32(13).float(message.overallPercent);
    }
    writer.uint32(18).fork();
    for (const v of message.perCorePercent) {
      writer.float(v);
    }
    writer.join();
    writer.uint32(26).fork();
    for (const v of message.loadAverage) {
      writer.float(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CpuInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCpuInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 13) {
            break;
          }

          message.overallPercent = reader.float();
          continue;
        }
        case 2: {
          if (tag === 21) {
            message.perCorePercent.push(reader.float());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.perCorePercent.push(reader.float());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag === 29) {
            message.loadAverage.push(reader.float());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.loadAverage.push(reader.float());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CpuInfo {
    return {
      overallPercent: isSet(object.overallPercent)
        ? globalThis.Number(object.overallPercent)
        : isSet(object.overall_percent)
        ? globalThis.Number(object.overall_percent)
        : 0,
      perCorePercent: globalThis.Array.isArray(object?.perCorePercent)
        ? object.perCorePercent.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.per_core_percent)
        ? object.per_core_percent.map((e: any) => globalThis.Number(e))
        : [],
      loadAverage: globalThis.Array.isArray(object?.loadAverage)
        ? object.loadAverage.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.load_average)
        ? object.load_average.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: CpuInfo): unknown {
    const obj: any = {};
    if (message.overallPercent !== 0) {
      obj.overallPercent = message.overallPercent;
    }
    if (message.perCorePercent?.length) {
      obj.perCorePercent = message.perCorePercent;
    }
    if (message.loadAverage?.length) {
      obj.loadAverage = message.loadAverage;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CpuInfo>, I>>(base?: I): CpuInfo {
    return CpuInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CpuInfo>, I>>(object: I): CpuInfo {
    const message = createBaseCpuInfo();
    message.overallPercent = object.overallPercent ?? 0;
    message.perCorePercent = object.perCorePercent?.map((e) => e) || [];
    message.loadAverage = object.loadAverage?.map((e) => e) || [];
    return message;
  },
};

function createBaseRamInfo(): RamInfo {
  return { totalBytes: 0, usedBytes: 0, percent: 0 };
}

export const RamInfo: MessageFns<RamInfo> = {
  encode(message: RamInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.totalBytes !== 0) {
      writer.uint32(8).uint64(message.totalBytes);
    }
    if (message.usedBytes !== 0) {
      writer.uint32(16).uint64(message.usedBytes);
    }
    if (message.percent !== 0) {
      writer.uint32(29).float(message.percent);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RamInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRamInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.totalBytes = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.usedBytes = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.percent = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RamInfo {
    return {
      totalBytes: isSet(object.totalBytes)
        ? globalThis.Number(object.totalBytes)
        : isSet(object.total_bytes)
        ? globalThis.Number(object.total_bytes)
        : 0,
      usedBytes: isSet(object.usedBytes)
        ? globalThis.Number(object.usedBytes)
        : isSet(object.used_bytes)
        ? globalThis.Number(object.used_bytes)
        : 0,
      percent: isSet(object.percent) ? globalThis.Number(object.percent) : 0,
    };
  },

  toJSON(message: RamInfo): unknown {
    const obj: any = {};
    if (message.totalBytes !== 0) {
      obj.totalBytes = Math.round(message.totalBytes);
    }
    if (message.usedBytes !== 0) {
      obj.usedBytes = Math.round(message.usedBytes);
    }
    if (message.percent !== 0) {
      obj.percent = message.percent;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RamInfo>, I>>(base?: I): RamInfo {
    return RamInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RamInfo>, I>>(object: I): RamInfo {
    const message = createBaseRamInfo();
    message.totalBytes = object.totalBytes ?? 0;
    message.usedBytes = object.usedBytes ?? 0;
    message.percent = object.percent ?? 0;
    return message;
  },
};

function createBaseDiskInfo(): DiskInfo {
  return { mount: "", totalBytes: 0, usedBytes: 0, percent: 0 };
}

export const DiskInfo: MessageFns<DiskInfo> = {
  encode(message: DiskInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.mount !== "") {
      writer.uint32(10).string(message.mount);
    }
    if (message.totalBytes !== 0) {
      writer.uint32(16).uint64(message.totalBytes);
    }
    if (message.usedBytes !== 0) {
      writer.uint32(24).uint64(message.usedBytes);
    }
    if (message.percent !== 0) {
      writer.uint32(37).float(message.percent);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DiskInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDiskInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.mount = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalBytes = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.usedBytes = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 37) {
            break;
          }

          message.percent = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DiskInfo {
    return {
      mount: isSet(object.mount) ? globalThis.String(object.mount) : "",
      totalBytes: isSet(object.totalBytes)
        ? globalThis.Number(object.totalBytes)
        : isSet(object.total_bytes)
        ? globalThis.Number(object.total_bytes)
        : 0,
      usedBytes: isSet(object.usedBytes)
        ? globalThis.Number(object.usedBytes)
        : isSet(object.used_bytes)
        ? globalThis.Number(object.used_bytes)
        : 0,
      percent: isSet(object.percent) ? globalThis.Number(object.percent) : 0,
    };
  },

  toJSON(message: DiskInfo): unknown {
    const obj: any = {};
    if (message.mount !== "") {
      obj.mount = message.mount;
    }
    if (message.totalBytes !== 0) {
      obj.totalBytes = Math.round(message.totalBytes);
    }
    if (message.usedBytes !== 0) {
      obj.usedBytes = Math.round(message.usedBytes);
    }
    if (message.percent !== 0) {
      obj.percent = message.percent;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DiskInfo>, I>>(base?: I): DiskInfo {
    return DiskInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DiskInfo>, I>>(object: I): DiskInfo {
    const message = createBaseDiskInfo();
    message.mount = object.mount ?? "";
    message.totalBytes = object.totalBytes ?? 0;
    message.usedBytes = object.usedBytes ?? 0;
    message.percent = object.percent ?? 0;
    return message;
  },
};

function createBaseDockerInfo(): DockerInfo {
  return { containers: 0, running: 0 };
}

export const DockerInfo: MessageFns<DockerInfo> = {
  encode(message: DockerInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.containers !== 0) {
      writer.uint32(8).uint32(message.containers);
    }
    if (message.running !== 0) {
      writer.uint32(16).uint32(message.running);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DockerInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDockerInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.containers = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.running = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DockerInfo {
    return {
      containers: isSet(object.containers) ? globalThis.Number(object.containers) : 0,
      running: isSet(object.running) ? globalThis.Number(object.running) : 0,
    };
  },

  toJSON(message: DockerInfo): unknown {
    const obj: any = {};
    if (message.containers !== 0) {
      obj.containers = Math.round(message.containers);
    }
    if (message.running !== 0) {
      obj.running = Math.round(message.running);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DockerInfo>, I>>(base?: I): DockerInfo {
    return DockerInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DockerInfo>, I>>(object: I): DockerInfo {
    const message = createBaseDockerInfo();
    message.containers = object.containers ?? 0;
    message.running = object.running ?? 0;
    return message;
  },
};

function createBaseNetworkInterface(): NetworkInterface {
  return { iface: "", rxBytes: 0, txBytes: 0 };
}

export const NetworkInterface: MessageFns<NetworkInterface> = {
  encode(message: NetworkInterface, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.iface !== "") {
      writer.uint32(10).string(message.iface);
    }
    if (message.rxBytes !== 0) {
      writer.uint32(16).uint64(message.rxBytes);
    }
    if (message.txBytes !== 0) {
      writer.uint32(24).uint64(message.txBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NetworkInterface {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNetworkInterface();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.iface = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.rxBytes = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.txBytes = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NetworkInterface {
    return {
      iface: isSet(object.iface) ? globalThis.String(object.iface) : "",
      rxBytes: isSet(object.rxBytes)
        ? globalThis.Number(object.rxBytes)
        : isSet(object.rx_bytes)
        ? globalThis.Number(object.rx_bytes)
        : 0,
      txBytes: isSet(object.txBytes)
        ? globalThis.Number(object.txBytes)
        : isSet(object.tx_bytes)
        ? globalThis.Number(object.tx_bytes)
        : 0,
    };
  },

  toJSON(message: NetworkInterface): unknown {
    const obj: any = {};
    if (message.iface !== "") {
      obj.iface = message.iface;
    }
    if (message.rxBytes !== 0) {
      obj.rxBytes = Math.round(message.rxBytes);
    }
    if (message.txBytes !== 0) {
      obj.txBytes = Math.round(message.txBytes);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NetworkInterface>, I>>(base?: I): NetworkInterface {
    return NetworkInterface.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NetworkInterface>, I>>(object: I): NetworkInterface {
    const message = createBaseNetworkInterface();
    message.iface = object.iface ?? "";
    message.rxBytes = object.rxBytes ?? 0;
    message.txBytes = object.txBytes ?? 0;
    return message;
  },
};

function createBaseProcessEntry(): ProcessEntry {
  return { pid: 0, name: "", cpuPercent: 0, ramPercent: 0 };
}

export const ProcessEntry: MessageFns<ProcessEntry> = {
  encode(message: ProcessEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pid !== 0) {
      writer.uint32(8).uint32(message.pid);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.cpuPercent !== 0) {
      writer.uint32(29).float(message.cpuPercent);
    }
    if (message.ramPercent !== 0) {
      writer.uint32(37).float(message.ramPercent);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProcessEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProcessEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pid = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.cpuPercent = reader.float();
          continue;
        }
        case 4: {
          if (tag !== 37) {
            break;
          }

          message.ramPercent = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProcessEntry {
    return {
      pid: isSet(object.pid) ? globalThis.Number(object.pid) : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      cpuPercent: isSet(object.cpuPercent)
        ? globalThis.Number(object.cpuPercent)
        : isSet(object.cpu_percent)
        ? globalThis.Number(object.cpu_percent)
        : 0,
      ramPercent: isSet(object.ramPercent)
        ? globalThis.Number(object.ramPercent)
        : isSet(object.ram_percent)
        ? globalThis.Number(object.ram_percent)
        : 0,
    };
  },

  toJSON(message: ProcessEntry): unknown {
    const obj: any = {};
    if (message.pid !== 0) {
      obj.pid = Math.round(message.pid);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.cpuPercent !== 0) {
      obj.cpuPercent = message.cpuPercent;
    }
    if (message.ramPercent !== 0) {
      obj.ramPercent = message.ramPercent;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProcessEntry>, I>>(base?: I): ProcessEntry {
    return ProcessEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProcessEntry>, I>>(object: I): ProcessEntry {
    const message = createBaseProcessEntry();
    message.pid = object.pid ?? 0;
    message.name = object.name ?? "";
    message.cpuPercent = object.cpuPercent ?? 0;
    message.ramPercent = object.ramPercent ?? 0;
    return message;
  },
};

function createBaseProcessSnapshot(): ProcessSnapshot {
  return { topCpu: [], topRam: [] };
}

export const ProcessSnapshot: MessageFns<ProcessSnapshot> = {
  encode(message: ProcessSnapshot, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.topCpu) {
      ProcessEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.topRam) {
      ProcessEntry.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProcessSnapshot {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProcessSnapshot();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.topCpu.push(ProcessEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.topRam.push(ProcessEntry.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProcessSnapshot {
    return {
      topCpu: globalThis.Array.isArray(object?.topCpu)
        ? object.topCpu.map((e: any) => ProcessEntry.fromJSON(e))
        : globalThis.Array.isArray(object?.top_cpu)
        ? object.top_cpu.map((e: any) => ProcessEntry.fromJSON(e))
        : [],
      topRam: globalThis.Array.isArray(object?.topRam)
        ? object.topRam.map((e: any) => ProcessEntry.fromJSON(e))
        : globalThis.Array.isArray(object?.top_ram)
        ? object.top_ram.map((e: any) => ProcessEntry.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ProcessSnapshot): unknown {
    const obj: any = {};
    if (message.topCpu?.length) {
      obj.topCpu = message.topCpu.map((e) => ProcessEntry.toJSON(e));
    }
    if (message.topRam?.length) {
      obj.topRam = message.topRam.map((e) => ProcessEntry.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProcessSnapshot>, I>>(base?: I): ProcessSnapshot {
    return ProcessSnapshot.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProcessSnapshot>, I>>(object: I): ProcessSnapshot {
    const message = createBaseProcessSnapshot();
    message.topCpu = object.topCpu?.map((e) => ProcessEntry.fromPartial(e)) || [];
    message.topRam = object.topRam?.map((e) => ProcessEntry.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMachineHealth(): MachineHealth {
  return {
    hostname: "",
    uptimeSeconds: 0,
    cpu: undefined,
    ram: undefined,
    disk: [],
    docker: undefined,
    dockerContainers: [],
    collectedAt: undefined,
    network: [],
    processes: undefined,
  };
}

export const MachineHealth: MessageFns<MachineHealth> = {
  encode(message: MachineHealth, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hostname !== "") {
      writer.uint32(10).string(message.hostname);
    }
    if (message.uptimeSeconds !== 0) {
      writer.uint32(16).uint64(message.uptimeSeconds);
    }
    if (message.cpu !== undefined) {
      CpuInfo.encode(message.cpu, writer.uint32(26).fork()).join();
    }
    if (message.ram !== undefined) {
      RamInfo.encode(message.ram, writer.uint32(34).fork()).join();
    }
    for (const v of message.disk) {
      DiskInfo.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.docker !== undefined) {
      DockerInfo.encode(message.docker, writer.uint32(50).fork()).join();
    }
    for (const v of message.dockerContainers) {
      ContainerStatus.encode(v!, writer.uint32(58).fork()).join();
    }
    if (message.collectedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.collectedAt), writer.uint32(66).fork()).join();
    }
    for (const v of message.network) {
      NetworkInterface.encode(v!, writer.uint32(74).fork()).join();
    }
    if (message.processes !== undefined) {
      ProcessSnapshot.encode(message.processes, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MachineHealth {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMachineHealth();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hostname = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.uptimeSeconds = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.cpu = CpuInfo.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.ram = RamInfo.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.disk.push(DiskInfo.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.docker = DockerInfo.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.dockerContainers.push(ContainerStatus.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.collectedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.network.push(NetworkInterface.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.processes = ProcessSnapshot.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MachineHealth {
    return {
      hostname: isSet(object.hostname) ? globalThis.String(object.hostname) : "",
      uptimeSeconds: isSet(object.uptimeSeconds)
        ? globalThis.Number(object.uptimeSeconds)
        : isSet(object.uptime_seconds)
        ? globalThis.Number(object.uptime_seconds)
        : 0,
      cpu: isSet(object.cpu) ? CpuInfo.fromJSON(object.cpu) : undefined,
      ram: isSet(object.ram) ? RamInfo.fromJSON(object.ram) : undefined,
      disk: globalThis.Array.isArray(object?.disk) ? object.disk.map((e: any) => DiskInfo.fromJSON(e)) : [],
      docker: isSet(object.docker) ? DockerInfo.fromJSON(object.docker) : undefined,
      dockerContainers: globalThis.Array.isArray(object?.dockerContainers)
        ? object.dockerContainers.map((e: any) => ContainerStatus.fromJSON(e))
        : globalThis.Array.isArray(object?.docker_containers)
        ? object.docker_containers.map((e: any) => ContainerStatus.fromJSON(e))
        : [],
      collectedAt: isSet(object.collectedAt)
        ? fromJsonTimestamp(object.collectedAt)
        : isSet(object.collected_at)
        ? fromJsonTimestamp(object.collected_at)
        : undefined,
      network: globalThis.Array.isArray(object?.network)
        ? object.network.map((e: any) => NetworkInterface.fromJSON(e))
        : [],
      processes: isSet(object.processes) ? ProcessSnapshot.fromJSON(object.processes) : undefined,
    };
  },

  toJSON(message: MachineHealth): unknown {
    const obj: any = {};
    if (message.hostname !== "") {
      obj.hostname = message.hostname;
    }
    if (message.uptimeSeconds !== 0) {
      obj.uptimeSeconds = Math.round(message.uptimeSeconds);
    }
    if (message.cpu !== undefined) {
      obj.cpu = CpuInfo.toJSON(message.cpu);
    }
    if (message.ram !== undefined) {
      obj.ram = RamInfo.toJSON(message.ram);
    }
    if (message.disk?.length) {
      obj.disk = message.disk.map((e) => DiskInfo.toJSON(e));
    }
    if (message.docker !== undefined) {
      obj.docker = DockerInfo.toJSON(message.docker);
    }
    if (message.dockerContainers?.length) {
      obj.dockerContainers = message.dockerContainers.map((e) => ContainerStatus.toJSON(e));
    }
    if (message.collectedAt !== undefined) {
      obj.collectedAt = message.collectedAt.toISOString();
    }
    if (message.network?.length) {
      obj.network = message.network.map((e) => NetworkInterface.toJSON(e));
    }
    if (message.processes !== undefined) {
      obj.processes = ProcessSnapshot.toJSON(message.processes);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MachineHealth>, I>>(base?: I): MachineHealth {
    return MachineHealth.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MachineHealth>, I>>(object: I): MachineHealth {
    const message = createBaseMachineHealth();
    message.hostname = object.hostname ?? "";
    message.uptimeSeconds = object.uptimeSeconds ?? 0;
    message.cpu = (object.cpu !== undefined && object.cpu !== null) ? CpuInfo.fromPartial(object.cpu) : undefined;
    message.ram = (object.ram !== undefined && object.ram !== null) ? RamInfo.fromPartial(object.ram) : undefined;
    message.disk = object.disk?.map((e) => DiskInfo.fromPartial(e)) || [];
    message.docker = (object.docker !== undefined && object.docker !== null)
      ? DockerInfo.fromPartial(object.docker)
      : undefined;
    message.dockerContainers = object.dockerContainers?.map((e) => ContainerStatus.fromPartial(e)) || [];
    message.collectedAt = object.collectedAt ?? undefined;
    message.network = object.network?.map((e) => NetworkInterface.fromPartial(e)) || [];
    message.processes = (object.processes !== undefined && object.processes !== null)
      ? ProcessSnapshot.fromPartial(object.processes)
      : undefined;
    return message;
  },
};

function createBaseHealthRequest(): HealthRequest {
  return {};
}

export const HealthRequest: MessageFns<HealthRequest> = {
  encode(_: HealthRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): HealthRequest {
    return {};
  },

  toJSON(_: HealthRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<HealthRequest>, I>>(base?: I): HealthRequest {
    return HealthRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HealthRequest>, I>>(_: I): HealthRequest {
    const message = createBaseHealthRequest();
    return message;
  },
};

function createBaseHealthResponse(): HealthResponse {
  return {
    agentName: "",
    agentHost: "",
    uptimeSeconds: 0,
    sessionCount: 0,
    machine: undefined,
    latestRateLimit: undefined,
  };
}

export const HealthResponse: MessageFns<HealthResponse> = {
  encode(message: HealthResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.agentName !== "") {
      writer.uint32(10).string(message.agentName);
    }
    if (message.agentHost !== "") {
      writer.uint32(18).string(message.agentHost);
    }
    if (message.uptimeSeconds !== 0) {
      writer.uint32(24).uint64(message.uptimeSeconds);
    }
    if (message.sessionCount !== 0) {
      writer.uint32(32).uint32(message.sessionCount);
    }
    if (message.machine !== undefined) {
      MachineHealth.encode(message.machine, writer.uint32(42).fork()).join();
    }
    if (message.latestRateLimit !== undefined) {
      RateLimitInfo.encode(message.latestRateLimit, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.agentName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.agentHost = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.uptimeSeconds = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.sessionCount = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.machine = MachineHealth.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.latestRateLimit = RateLimitInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HealthResponse {
    return {
      agentName: isSet(object.agentName)
        ? globalThis.String(object.agentName)
        : isSet(object.agent_name)
        ? globalThis.String(object.agent_name)
        : "",
      agentHost: isSet(object.agentHost)
        ? globalThis.String(object.agentHost)
        : isSet(object.agent_host)
        ? globalThis.String(object.agent_host)
        : "",
      uptimeSeconds: isSet(object.uptimeSeconds)
        ? globalThis.Number(object.uptimeSeconds)
        : isSet(object.uptime_seconds)
        ? globalThis.Number(object.uptime_seconds)
        : 0,
      sessionCount: isSet(object.sessionCount)
        ? globalThis.Number(object.sessionCount)
        : isSet(object.session_count)
        ? globalThis.Number(object.session_count)
        : 0,
      machine: isSet(object.machine) ? MachineHealth.fromJSON(object.machine) : undefined,
      latestRateLimit: isSet(object.latestRateLimit)
        ? RateLimitInfo.fromJSON(object.latestRateLimit)
        : isSet(object.latest_rate_limit)
        ? RateLimitInfo.fromJSON(object.latest_rate_limit)
        : undefined,
    };
  },

  toJSON(message: HealthResponse): unknown {
    const obj: any = {};
    if (message.agentName !== "") {
      obj.agentName = message.agentName;
    }
    if (message.agentHost !== "") {
      obj.agentHost = message.agentHost;
    }
    if (message.uptimeSeconds !== 0) {
      obj.uptimeSeconds = Math.round(message.uptimeSeconds);
    }
    if (message.sessionCount !== 0) {
      obj.sessionCount = Math.round(message.sessionCount);
    }
    if (message.machine !== undefined) {
      obj.machine = MachineHealth.toJSON(message.machine);
    }
    if (message.latestRateLimit !== undefined) {
      obj.latestRateLimit = RateLimitInfo.toJSON(message.latestRateLimit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HealthResponse>, I>>(base?: I): HealthResponse {
    return HealthResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HealthResponse>, I>>(object: I): HealthResponse {
    const message = createBaseHealthResponse();
    message.agentName = object.agentName ?? "";
    message.agentHost = object.agentHost ?? "";
    message.uptimeSeconds = object.uptimeSeconds ?? 0;
    message.sessionCount = object.sessionCount ?? 0;
    message.machine = (object.machine !== undefined && object.machine !== null)
      ? MachineHealth.fromPartial(object.machine)
      : undefined;
    message.latestRateLimit = (object.latestRateLimit !== undefined && object.latestRateLimit !== null)
      ? RateLimitInfo.fromPartial(object.latestRateLimit)
      : undefined;
    return message;
  },
};

function createBaseBeadsStatus(): BeadsStatus {
  return { readyCount: 0, openCount: 0, blockedCount: 0, readyJson: "" };
}

export const BeadsStatus: MessageFns<BeadsStatus> = {
  encode(message: BeadsStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.readyCount !== 0) {
      writer.uint32(8).int32(message.readyCount);
    }
    if (message.openCount !== 0) {
      writer.uint32(16).int32(message.openCount);
    }
    if (message.blockedCount !== 0) {
      writer.uint32(24).int32(message.blockedCount);
    }
    if (message.readyJson !== "") {
      writer.uint32(34).string(message.readyJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BeadsStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBeadsStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.readyCount = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.openCount = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.blockedCount = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.readyJson = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BeadsStatus {
    return {
      readyCount: isSet(object.readyCount)
        ? globalThis.Number(object.readyCount)
        : isSet(object.ready_count)
        ? globalThis.Number(object.ready_count)
        : 0,
      openCount: isSet(object.openCount)
        ? globalThis.Number(object.openCount)
        : isSet(object.open_count)
        ? globalThis.Number(object.open_count)
        : 0,
      blockedCount: isSet(object.blockedCount)
        ? globalThis.Number(object.blockedCount)
        : isSet(object.blocked_count)
        ? globalThis.Number(object.blocked_count)
        : 0,
      readyJson: isSet(object.readyJson)
        ? globalThis.String(object.readyJson)
        : isSet(object.ready_json)
        ? globalThis.String(object.ready_json)
        : "",
    };
  },

  toJSON(message: BeadsStatus): unknown {
    const obj: any = {};
    if (message.readyCount !== 0) {
      obj.readyCount = Math.round(message.readyCount);
    }
    if (message.openCount !== 0) {
      obj.openCount = Math.round(message.openCount);
    }
    if (message.blockedCount !== 0) {
      obj.blockedCount = Math.round(message.blockedCount);
    }
    if (message.readyJson !== "") {
      obj.readyJson = message.readyJson;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BeadsStatus>, I>>(base?: I): BeadsStatus {
    return BeadsStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BeadsStatus>, I>>(object: I): BeadsStatus {
    const message = createBaseBeadsStatus();
    message.readyCount = object.readyCount ?? 0;
    message.openCount = object.openCount ?? 0;
    message.blockedCount = object.blockedCount ?? 0;
    message.readyJson = object.readyJson ?? "";
    return message;
  },
};

function createBaseGitStatusInfo(): GitStatusInfo {
  return { branch: "", headSha: "", recentCommits: [], porcelain: "" };
}

export const GitStatusInfo: MessageFns<GitStatusInfo> = {
  encode(message: GitStatusInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.branch !== "") {
      writer.uint32(10).string(message.branch);
    }
    if (message.headSha !== "") {
      writer.uint32(18).string(message.headSha);
    }
    for (const v of message.recentCommits) {
      writer.uint32(26).string(v!);
    }
    if (message.porcelain !== "") {
      writer.uint32(34).string(message.porcelain);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GitStatusInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGitStatusInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.branch = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.headSha = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.recentCommits.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.porcelain = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GitStatusInfo {
    return {
      branch: isSet(object.branch) ? globalThis.String(object.branch) : "",
      headSha: isSet(object.headSha)
        ? globalThis.String(object.headSha)
        : isSet(object.head_sha)
        ? globalThis.String(object.head_sha)
        : "",
      recentCommits: globalThis.Array.isArray(object?.recentCommits)
        ? object.recentCommits.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.recent_commits)
        ? object.recent_commits.map((e: any) => globalThis.String(e))
        : [],
      porcelain: isSet(object.porcelain) ? globalThis.String(object.porcelain) : "",
    };
  },

  toJSON(message: GitStatusInfo): unknown {
    const obj: any = {};
    if (message.branch !== "") {
      obj.branch = message.branch;
    }
    if (message.headSha !== "") {
      obj.headSha = message.headSha;
    }
    if (message.recentCommits?.length) {
      obj.recentCommits = message.recentCommits;
    }
    if (message.porcelain !== "") {
      obj.porcelain = message.porcelain;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GitStatusInfo>, I>>(base?: I): GitStatusInfo {
    return GitStatusInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GitStatusInfo>, I>>(object: I): GitStatusInfo {
    const message = createBaseGitStatusInfo();
    message.branch = object.branch ?? "";
    message.headSha = object.headSha ?? "";
    message.recentCommits = object.recentCommits?.map((e) => e) || [];
    message.porcelain = object.porcelain ?? "";
    return message;
  },
};

function createBaseSpecStatusInfo(): SpecStatusInfo {
  return { specCount: 0, changeCount: 0, activeChanges: [] };
}

export const SpecStatusInfo: MessageFns<SpecStatusInfo> = {
  encode(message: SpecStatusInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.specCount !== 0) {
      writer.uint32(8).int32(message.specCount);
    }
    if (message.changeCount !== 0) {
      writer.uint32(16).int32(message.changeCount);
    }
    for (const v of message.activeChanges) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SpecStatusInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSpecStatusInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.specCount = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.changeCount = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.activeChanges.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SpecStatusInfo {
    return {
      specCount: isSet(object.specCount)
        ? globalThis.Number(object.specCount)
        : isSet(object.spec_count)
        ? globalThis.Number(object.spec_count)
        : 0,
      changeCount: isSet(object.changeCount)
        ? globalThis.Number(object.changeCount)
        : isSet(object.change_count)
        ? globalThis.Number(object.change_count)
        : 0,
      activeChanges: globalThis.Array.isArray(object?.activeChanges)
        ? object.activeChanges.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.active_changes)
        ? object.active_changes.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: SpecStatusInfo): unknown {
    const obj: any = {};
    if (message.specCount !== 0) {
      obj.specCount = Math.round(message.specCount);
    }
    if (message.changeCount !== 0) {
      obj.changeCount = Math.round(message.changeCount);
    }
    if (message.activeChanges?.length) {
      obj.activeChanges = message.activeChanges;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SpecStatusInfo>, I>>(base?: I): SpecStatusInfo {
    return SpecStatusInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SpecStatusInfo>, I>>(object: I): SpecStatusInfo {
    const message = createBaseSpecStatusInfo();
    message.specCount = object.specCount ?? 0;
    message.changeCount = object.changeCount ?? 0;
    message.activeChanges = object.activeChanges?.map((e) => e) || [];
    return message;
  },
};

function createBaseProjectStatusRequest(): ProjectStatusRequest {
  return { project: "", fresh: false };
}

export const ProjectStatusRequest: MessageFns<ProjectStatusRequest> = {
  encode(message: ProjectStatusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.project !== "") {
      writer.uint32(10).string(message.project);
    }
    if (message.fresh !== false) {
      writer.uint32(16).bool(message.fresh);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProjectStatusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProjectStatusRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.project = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.fresh = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProjectStatusRequest {
    return {
      project: isSet(object.project) ? globalThis.String(object.project) : "",
      fresh: isSet(object.fresh) ? globalThis.Boolean(object.fresh) : false,
    };
  },

  toJSON(message: ProjectStatusRequest): unknown {
    const obj: any = {};
    if (message.project !== "") {
      obj.project = message.project;
    }
    if (message.fresh !== false) {
      obj.fresh = message.fresh;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProjectStatusRequest>, I>>(base?: I): ProjectStatusRequest {
    return ProjectStatusRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProjectStatusRequest>, I>>(object: I): ProjectStatusRequest {
    const message = createBaseProjectStatusRequest();
    message.project = object.project ?? "";
    message.fresh = object.fresh ?? false;
    return message;
  },
};

function createBaseProjectStatusResponse(): ProjectStatusResponse {
  return { project: "", beads: undefined, git: undefined, specs: undefined };
}

export const ProjectStatusResponse: MessageFns<ProjectStatusResponse> = {
  encode(message: ProjectStatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.project !== "") {
      writer.uint32(10).string(message.project);
    }
    if (message.beads !== undefined) {
      BeadsStatus.encode(message.beads, writer.uint32(18).fork()).join();
    }
    if (message.git !== undefined) {
      GitStatusInfo.encode(message.git, writer.uint32(26).fork()).join();
    }
    if (message.specs !== undefined) {
      SpecStatusInfo.encode(message.specs, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProjectStatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProjectStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.project = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.beads = BeadsStatus.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.git = GitStatusInfo.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.specs = SpecStatusInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProjectStatusResponse {
    return {
      project: isSet(object.project) ? globalThis.String(object.project) : "",
      beads: isSet(object.beads) ? BeadsStatus.fromJSON(object.beads) : undefined,
      git: isSet(object.git) ? GitStatusInfo.fromJSON(object.git) : undefined,
      specs: isSet(object.specs) ? SpecStatusInfo.fromJSON(object.specs) : undefined,
    };
  },

  toJSON(message: ProjectStatusResponse): unknown {
    const obj: any = {};
    if (message.project !== "") {
      obj.project = message.project;
    }
    if (message.beads !== undefined) {
      obj.beads = BeadsStatus.toJSON(message.beads);
    }
    if (message.git !== undefined) {
      obj.git = GitStatusInfo.toJSON(message.git);
    }
    if (message.specs !== undefined) {
      obj.specs = SpecStatusInfo.toJSON(message.specs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProjectStatusResponse>, I>>(base?: I): ProjectStatusResponse {
    return ProjectStatusResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProjectStatusResponse>, I>>(object: I): ProjectStatusResponse {
    const message = createBaseProjectStatusResponse();
    message.project = object.project ?? "";
    message.beads = (object.beads !== undefined && object.beads !== null)
      ? BeadsStatus.fromPartial(object.beads)
      : undefined;
    message.git = (object.git !== undefined && object.git !== null) ? GitStatusInfo.fromPartial(object.git) : undefined;
    message.specs = (object.specs !== undefined && object.specs !== null)
      ? SpecStatusInfo.fromPartial(object.specs)
      : undefined;
    return message;
  },
};

function createBaseCommandInfoProto(): CommandInfoProto {
  return { name: "", namespace: "", fullName: "", description: "", tier: 0, cost: 0 };
}

export const CommandInfoProto: MessageFns<CommandInfoProto> = {
  encode(message: CommandInfoProto, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.namespace !== "") {
      writer.uint32(18).string(message.namespace);
    }
    if (message.fullName !== "") {
      writer.uint32(26).string(message.fullName);
    }
    if (message.description !== "") {
      writer.uint32(34).string(message.description);
    }
    if (message.tier !== 0) {
      writer.uint32(40).int32(message.tier);
    }
    if (message.cost !== 0) {
      writer.uint32(48).int32(message.cost);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommandInfoProto {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommandInfoProto();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.namespace = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.fullName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.tier = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.cost = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CommandInfoProto {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      namespace: isSet(object.namespace) ? globalThis.String(object.namespace) : "",
      fullName: isSet(object.fullName)
        ? globalThis.String(object.fullName)
        : isSet(object.full_name)
        ? globalThis.String(object.full_name)
        : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      tier: isSet(object.tier) ? commandTierFromJSON(object.tier) : 0,
      cost: isSet(object.cost) ? costCategoryFromJSON(object.cost) : 0,
    };
  },

  toJSON(message: CommandInfoProto): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.namespace !== "") {
      obj.namespace = message.namespace;
    }
    if (message.fullName !== "") {
      obj.fullName = message.fullName;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.tier !== 0) {
      obj.tier = commandTierToJSON(message.tier);
    }
    if (message.cost !== 0) {
      obj.cost = costCategoryToJSON(message.cost);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CommandInfoProto>, I>>(base?: I): CommandInfoProto {
    return CommandInfoProto.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CommandInfoProto>, I>>(object: I): CommandInfoProto {
    const message = createBaseCommandInfoProto();
    message.name = object.name ?? "";
    message.namespace = object.namespace ?? "";
    message.fullName = object.fullName ?? "";
    message.description = object.description ?? "";
    message.tier = object.tier ?? 0;
    message.cost = object.cost ?? 0;
    return message;
  },
};

function createBaseListCommandsRequest(): ListCommandsRequest {
  return { namespace: undefined, tier: undefined };
}

export const ListCommandsRequest: MessageFns<ListCommandsRequest> = {
  encode(message: ListCommandsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.namespace !== undefined) {
      writer.uint32(10).string(message.namespace);
    }
    if (message.tier !== undefined) {
      writer.uint32(16).int32(message.tier);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListCommandsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListCommandsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.namespace = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.tier = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListCommandsRequest {
    return {
      namespace: isSet(object.namespace) ? globalThis.String(object.namespace) : undefined,
      tier: isSet(object.tier) ? commandTierFromJSON(object.tier) : undefined,
    };
  },

  toJSON(message: ListCommandsRequest): unknown {
    const obj: any = {};
    if (message.namespace !== undefined) {
      obj.namespace = message.namespace;
    }
    if (message.tier !== undefined) {
      obj.tier = commandTierToJSON(message.tier);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListCommandsRequest>, I>>(base?: I): ListCommandsRequest {
    return ListCommandsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListCommandsRequest>, I>>(object: I): ListCommandsRequest {
    const message = createBaseListCommandsRequest();
    message.namespace = object.namespace ?? undefined;
    message.tier = object.tier ?? undefined;
    return message;
  },
};

function createBaseListCommandsResponse(): ListCommandsResponse {
  return { commands: [] };
}

export const ListCommandsResponse: MessageFns<ListCommandsResponse> = {
  encode(message: ListCommandsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.commands) {
      CommandInfoProto.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListCommandsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListCommandsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.commands.push(CommandInfoProto.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListCommandsResponse {
    return {
      commands: globalThis.Array.isArray(object?.commands)
        ? object.commands.map((e: any) => CommandInfoProto.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListCommandsResponse): unknown {
    const obj: any = {};
    if (message.commands?.length) {
      obj.commands = message.commands.map((e) => CommandInfoProto.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListCommandsResponse>, I>>(base?: I): ListCommandsResponse {
    return ListCommandsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListCommandsResponse>, I>>(object: I): ListCommandsResponse {
    const message = createBaseListCommandsResponse();
    message.commands = object.commands?.map((e) => CommandInfoProto.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRunProjectCommandRequest(): RunProjectCommandRequest {
  return { project: "", command: "", args: [], freshSession: false };
}

export const RunProjectCommandRequest: MessageFns<RunProjectCommandRequest> = {
  encode(message: RunProjectCommandRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.project !== "") {
      writer.uint32(10).string(message.project);
    }
    if (message.command !== "") {
      writer.uint32(18).string(message.command);
    }
    for (const v of message.args) {
      writer.uint32(26).string(v!);
    }
    if (message.freshSession !== false) {
      writer.uint32(32).bool(message.freshSession);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RunProjectCommandRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRunProjectCommandRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.project = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.command = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.args.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.freshSession = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RunProjectCommandRequest {
    return {
      project: isSet(object.project) ? globalThis.String(object.project) : "",
      command: isSet(object.command) ? globalThis.String(object.command) : "",
      args: globalThis.Array.isArray(object?.args) ? object.args.map((e: any) => globalThis.String(e)) : [],
      freshSession: isSet(object.freshSession)
        ? globalThis.Boolean(object.freshSession)
        : isSet(object.fresh_session)
        ? globalThis.Boolean(object.fresh_session)
        : false,
    };
  },

  toJSON(message: RunProjectCommandRequest): unknown {
    const obj: any = {};
    if (message.project !== "") {
      obj.project = message.project;
    }
    if (message.command !== "") {
      obj.command = message.command;
    }
    if (message.args?.length) {
      obj.args = message.args;
    }
    if (message.freshSession !== false) {
      obj.freshSession = message.freshSession;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RunProjectCommandRequest>, I>>(base?: I): RunProjectCommandRequest {
    return RunProjectCommandRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RunProjectCommandRequest>, I>>(object: I): RunProjectCommandRequest {
    const message = createBaseRunProjectCommandRequest();
    message.project = object.project ?? "";
    message.command = object.command ?? "";
    message.args = object.args?.map((e) => e) || [];
    message.freshSession = object.freshSession ?? false;
    return message;
  },
};

function createBaseSessionHistoryRequest(): SessionHistoryRequest {
  return { days: 0, project: undefined };
}

export const SessionHistoryRequest: MessageFns<SessionHistoryRequest> = {
  encode(message: SessionHistoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.days !== 0) {
      writer.uint32(8).uint32(message.days);
    }
    if (message.project !== undefined) {
      writer.uint32(18).string(message.project);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SessionHistoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSessionHistoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.days = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.project = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SessionHistoryRequest {
    return {
      days: isSet(object.days) ? globalThis.Number(object.days) : 0,
      project: isSet(object.project) ? globalThis.String(object.project) : undefined,
    };
  },

  toJSON(message: SessionHistoryRequest): unknown {
    const obj: any = {};
    if (message.days !== 0) {
      obj.days = Math.round(message.days);
    }
    if (message.project !== undefined) {
      obj.project = message.project;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SessionHistoryRequest>, I>>(base?: I): SessionHistoryRequest {
    return SessionHistoryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SessionHistoryRequest>, I>>(object: I): SessionHistoryRequest {
    const message = createBaseSessionHistoryRequest();
    message.days = object.days ?? 0;
    message.project = object.project ?? undefined;
    return message;
  },
};

function createBaseSessionHistoryEntry(): SessionHistoryEntry {
  return { date: "", sessionCount: 0, totalCostUsd: 0 };
}

export const SessionHistoryEntry: MessageFns<SessionHistoryEntry> = {
  encode(message: SessionHistoryEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.date !== "") {
      writer.uint32(10).string(message.date);
    }
    if (message.sessionCount !== 0) {
      writer.uint32(16).int32(message.sessionCount);
    }
    if (message.totalCostUsd !== 0) {
      writer.uint32(25).double(message.totalCostUsd);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SessionHistoryEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSessionHistoryEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.date = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sessionCount = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.totalCostUsd = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SessionHistoryEntry {
    return {
      date: isSet(object.date) ? globalThis.String(object.date) : "",
      sessionCount: isSet(object.sessionCount)
        ? globalThis.Number(object.sessionCount)
        : isSet(object.session_count)
        ? globalThis.Number(object.session_count)
        : 0,
      totalCostUsd: isSet(object.totalCostUsd)
        ? globalThis.Number(object.totalCostUsd)
        : isSet(object.total_cost_usd)
        ? globalThis.Number(object.total_cost_usd)
        : 0,
    };
  },

  toJSON(message: SessionHistoryEntry): unknown {
    const obj: any = {};
    if (message.date !== "") {
      obj.date = message.date;
    }
    if (message.sessionCount !== 0) {
      obj.sessionCount = Math.round(message.sessionCount);
    }
    if (message.totalCostUsd !== 0) {
      obj.totalCostUsd = message.totalCostUsd;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SessionHistoryEntry>, I>>(base?: I): SessionHistoryEntry {
    return SessionHistoryEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SessionHistoryEntry>, I>>(object: I): SessionHistoryEntry {
    const message = createBaseSessionHistoryEntry();
    message.date = object.date ?? "";
    message.sessionCount = object.sessionCount ?? 0;
    message.totalCostUsd = object.totalCostUsd ?? 0;
    return message;
  },
};

function createBaseSessionHistoryResponse(): SessionHistoryResponse {
  return { entries: [] };
}

export const SessionHistoryResponse: MessageFns<SessionHistoryResponse> = {
  encode(message: SessionHistoryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.entries) {
      SessionHistoryEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SessionHistoryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSessionHistoryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entries.push(SessionHistoryEntry.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SessionHistoryResponse {
    return {
      entries: globalThis.Array.isArray(object?.entries)
        ? object.entries.map((e: any) => SessionHistoryEntry.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SessionHistoryResponse): unknown {
    const obj: any = {};
    if (message.entries?.length) {
      obj.entries = message.entries.map((e) => SessionHistoryEntry.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SessionHistoryResponse>, I>>(base?: I): SessionHistoryResponse {
    return SessionHistoryResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SessionHistoryResponse>, I>>(object: I): SessionHistoryResponse {
    const message = createBaseSessionHistoryResponse();
    message.entries = object.entries?.map((e) => SessionHistoryEntry.fromPartial(e)) || [];
    return message;
  },
};

function createBaseFailureTrendsRequest(): FailureTrendsRequest {
  return { days: 0 };
}

export const FailureTrendsRequest: MessageFns<FailureTrendsRequest> = {
  encode(message: FailureTrendsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.days !== 0) {
      writer.uint32(8).uint32(message.days);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FailureTrendsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFailureTrendsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.days = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FailureTrendsRequest {
    return { days: isSet(object.days) ? globalThis.Number(object.days) : 0 };
  },

  toJSON(message: FailureTrendsRequest): unknown {
    const obj: any = {};
    if (message.days !== 0) {
      obj.days = Math.round(message.days);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FailureTrendsRequest>, I>>(base?: I): FailureTrendsRequest {
    return FailureTrendsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FailureTrendsRequest>, I>>(object: I): FailureTrendsRequest {
    const message = createBaseFailureTrendsRequest();
    message.days = object.days ?? 0;
    return message;
  },
};

function createBaseFailureTrendEntry(): FailureTrendEntry {
  return { date: "", count: 0 };
}

export const FailureTrendEntry: MessageFns<FailureTrendEntry> = {
  encode(message: FailureTrendEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.date !== "") {
      writer.uint32(10).string(message.date);
    }
    if (message.count !== 0) {
      writer.uint32(16).int32(message.count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FailureTrendEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFailureTrendEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.date = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.count = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FailureTrendEntry {
    return {
      date: isSet(object.date) ? globalThis.String(object.date) : "",
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
    };
  },

  toJSON(message: FailureTrendEntry): unknown {
    const obj: any = {};
    if (message.date !== "") {
      obj.date = message.date;
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FailureTrendEntry>, I>>(base?: I): FailureTrendEntry {
    return FailureTrendEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FailureTrendEntry>, I>>(object: I): FailureTrendEntry {
    const message = createBaseFailureTrendEntry();
    message.date = object.date ?? "";
    message.count = object.count ?? 0;
    return message;
  },
};

function createBaseFailureByTool(): FailureByTool {
  return { toolName: "", count: 0 };
}

export const FailureByTool: MessageFns<FailureByTool> = {
  encode(message: FailureByTool, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.toolName !== "") {
      writer.uint32(10).string(message.toolName);
    }
    if (message.count !== 0) {
      writer.uint32(16).int32(message.count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FailureByTool {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFailureByTool();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.toolName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.count = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FailureByTool {
    return {
      toolName: isSet(object.toolName)
        ? globalThis.String(object.toolName)
        : isSet(object.tool_name)
        ? globalThis.String(object.tool_name)
        : "",
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
    };
  },

  toJSON(message: FailureByTool): unknown {
    const obj: any = {};
    if (message.toolName !== "") {
      obj.toolName = message.toolName;
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FailureByTool>, I>>(base?: I): FailureByTool {
    return FailureByTool.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FailureByTool>, I>>(object: I): FailureByTool {
    const message = createBaseFailureByTool();
    message.toolName = object.toolName ?? "";
    message.count = object.count ?? 0;
    return message;
  },
};

function createBaseFailureTrendsResponse(): FailureTrendsResponse {
  return { daily: [], byTool: [] };
}

export const FailureTrendsResponse: MessageFns<FailureTrendsResponse> = {
  encode(message: FailureTrendsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.daily) {
      FailureTrendEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.byTool) {
      FailureByTool.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FailureTrendsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFailureTrendsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.daily.push(FailureTrendEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.byTool.push(FailureByTool.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FailureTrendsResponse {
    return {
      daily: globalThis.Array.isArray(object?.daily) ? object.daily.map((e: any) => FailureTrendEntry.fromJSON(e)) : [],
      byTool: globalThis.Array.isArray(object?.byTool)
        ? object.byTool.map((e: any) => FailureByTool.fromJSON(e))
        : globalThis.Array.isArray(object?.by_tool)
        ? object.by_tool.map((e: any) => FailureByTool.fromJSON(e))
        : [],
    };
  },

  toJSON(message: FailureTrendsResponse): unknown {
    const obj: any = {};
    if (message.daily?.length) {
      obj.daily = message.daily.map((e) => FailureTrendEntry.toJSON(e));
    }
    if (message.byTool?.length) {
      obj.byTool = message.byTool.map((e) => FailureByTool.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FailureTrendsResponse>, I>>(base?: I): FailureTrendsResponse {
    return FailureTrendsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FailureTrendsResponse>, I>>(object: I): FailureTrendsResponse {
    const message = createBaseFailureTrendsResponse();
    message.daily = object.daily?.map((e) => FailureTrendEntry.fromPartial(e)) || [];
    message.byTool = object.byTool?.map((e) => FailureByTool.fromPartial(e)) || [];
    return message;
  },
};

function createBaseHealthTimeSeriesRequest(): HealthTimeSeriesRequest {
  return { hours: 0 };
}

export const HealthTimeSeriesRequest: MessageFns<HealthTimeSeriesRequest> = {
  encode(message: HealthTimeSeriesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hours !== 0) {
      writer.uint32(8).uint32(message.hours);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthTimeSeriesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthTimeSeriesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.hours = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HealthTimeSeriesRequest {
    return { hours: isSet(object.hours) ? globalThis.Number(object.hours) : 0 };
  },

  toJSON(message: HealthTimeSeriesRequest): unknown {
    const obj: any = {};
    if (message.hours !== 0) {
      obj.hours = Math.round(message.hours);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HealthTimeSeriesRequest>, I>>(base?: I): HealthTimeSeriesRequest {
    return HealthTimeSeriesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HealthTimeSeriesRequest>, I>>(object: I): HealthTimeSeriesRequest {
    const message = createBaseHealthTimeSeriesRequest();
    message.hours = object.hours ?? 0;
    return message;
  },
};

function createBaseHealthTimeSeriesEntry(): HealthTimeSeriesEntry {
  return { timestamp: "", cpuPercent: 0, memoryUsedGb: 0, diskUsedGb: 0, load1: 0 };
}

export const HealthTimeSeriesEntry: MessageFns<HealthTimeSeriesEntry> = {
  encode(message: HealthTimeSeriesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timestamp !== "") {
      writer.uint32(10).string(message.timestamp);
    }
    if (message.cpuPercent !== 0) {
      writer.uint32(21).float(message.cpuPercent);
    }
    if (message.memoryUsedGb !== 0) {
      writer.uint32(29).float(message.memoryUsedGb);
    }
    if (message.diskUsedGb !== 0) {
      writer.uint32(37).float(message.diskUsedGb);
    }
    if (message.load1 !== 0) {
      writer.uint32(45).float(message.load1);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthTimeSeriesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthTimeSeriesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.timestamp = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 21) {
            break;
          }

          message.cpuPercent = reader.float();
          continue;
        }
        case 3: {
          if (tag !== 29) {
            break;
          }

          message.memoryUsedGb = reader.float();
          continue;
        }
        case 4: {
          if (tag !== 37) {
            break;
          }

          message.diskUsedGb = reader.float();
          continue;
        }
        case 5: {
          if (tag !== 45) {
            break;
          }

          message.load1 = reader.float();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HealthTimeSeriesEntry {
    return {
      timestamp: isSet(object.timestamp) ? globalThis.String(object.timestamp) : "",
      cpuPercent: isSet(object.cpuPercent)
        ? globalThis.Number(object.cpuPercent)
        : isSet(object.cpu_percent)
        ? globalThis.Number(object.cpu_percent)
        : 0,
      memoryUsedGb: isSet(object.memoryUsedGb)
        ? globalThis.Number(object.memoryUsedGb)
        : isSet(object.memory_used_gb)
        ? globalThis.Number(object.memory_used_gb)
        : 0,
      diskUsedGb: isSet(object.diskUsedGb)
        ? globalThis.Number(object.diskUsedGb)
        : isSet(object.disk_used_gb)
        ? globalThis.Number(object.disk_used_gb)
        : 0,
      load1: isSet(object.load1) ? globalThis.Number(object.load1) : 0,
    };
  },

  toJSON(message: HealthTimeSeriesEntry): unknown {
    const obj: any = {};
    if (message.timestamp !== "") {
      obj.timestamp = message.timestamp;
    }
    if (message.cpuPercent !== 0) {
      obj.cpuPercent = message.cpuPercent;
    }
    if (message.memoryUsedGb !== 0) {
      obj.memoryUsedGb = message.memoryUsedGb;
    }
    if (message.diskUsedGb !== 0) {
      obj.diskUsedGb = message.diskUsedGb;
    }
    if (message.load1 !== 0) {
      obj.load1 = message.load1;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HealthTimeSeriesEntry>, I>>(base?: I): HealthTimeSeriesEntry {
    return HealthTimeSeriesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HealthTimeSeriesEntry>, I>>(object: I): HealthTimeSeriesEntry {
    const message = createBaseHealthTimeSeriesEntry();
    message.timestamp = object.timestamp ?? "";
    message.cpuPercent = object.cpuPercent ?? 0;
    message.memoryUsedGb = object.memoryUsedGb ?? 0;
    message.diskUsedGb = object.diskUsedGb ?? 0;
    message.load1 = object.load1 ?? 0;
    return message;
  },
};

function createBaseHealthTimeSeriesResponse(): HealthTimeSeriesResponse {
  return { samples: [] };
}

export const HealthTimeSeriesResponse: MessageFns<HealthTimeSeriesResponse> = {
  encode(message: HealthTimeSeriesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.samples) {
      HealthTimeSeriesEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthTimeSeriesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthTimeSeriesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.samples.push(HealthTimeSeriesEntry.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HealthTimeSeriesResponse {
    return {
      samples: globalThis.Array.isArray(object?.samples)
        ? object.samples.map((e: any) => HealthTimeSeriesEntry.fromJSON(e))
        : [],
    };
  },

  toJSON(message: HealthTimeSeriesResponse): unknown {
    const obj: any = {};
    if (message.samples?.length) {
      obj.samples = message.samples.map((e) => HealthTimeSeriesEntry.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HealthTimeSeriesResponse>, I>>(base?: I): HealthTimeSeriesResponse {
    return HealthTimeSeriesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HealthTimeSeriesResponse>, I>>(object: I): HealthTimeSeriesResponse {
    const message = createBaseHealthTimeSeriesResponse();
    message.samples = object.samples?.map((e) => HealthTimeSeriesEntry.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSpecVelocityRequest(): SpecVelocityRequest {
  return { days: 0, project: undefined };
}

export const SpecVelocityRequest: MessageFns<SpecVelocityRequest> = {
  encode(message: SpecVelocityRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.days !== 0) {
      writer.uint32(8).uint32(message.days);
    }
    if (message.project !== undefined) {
      writer.uint32(18).string(message.project);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SpecVelocityRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSpecVelocityRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.days = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.project = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SpecVelocityRequest {
    return {
      days: isSet(object.days) ? globalThis.Number(object.days) : 0,
      project: isSet(object.project) ? globalThis.String(object.project) : undefined,
    };
  },

  toJSON(message: SpecVelocityRequest): unknown {
    const obj: any = {};
    if (message.days !== 0) {
      obj.days = Math.round(message.days);
    }
    if (message.project !== undefined) {
      obj.project = message.project;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SpecVelocityRequest>, I>>(base?: I): SpecVelocityRequest {
    return SpecVelocityRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SpecVelocityRequest>, I>>(object: I): SpecVelocityRequest {
    const message = createBaseSpecVelocityRequest();
    message.days = object.days ?? 0;
    message.project = object.project ?? undefined;
    return message;
  },
};

function createBaseSpecVelocityEntry(): SpecVelocityEntry {
  return { date: "", project: "", specName: "", completedTasks: 0, totalTasks: 0 };
}

export const SpecVelocityEntry: MessageFns<SpecVelocityEntry> = {
  encode(message: SpecVelocityEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.date !== "") {
      writer.uint32(10).string(message.date);
    }
    if (message.project !== "") {
      writer.uint32(18).string(message.project);
    }
    if (message.specName !== "") {
      writer.uint32(26).string(message.specName);
    }
    if (message.completedTasks !== 0) {
      writer.uint32(32).int32(message.completedTasks);
    }
    if (message.totalTasks !== 0) {
      writer.uint32(40).int32(message.totalTasks);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SpecVelocityEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSpecVelocityEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.date = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.project = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.specName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.completedTasks = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.totalTasks = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SpecVelocityEntry {
    return {
      date: isSet(object.date) ? globalThis.String(object.date) : "",
      project: isSet(object.project) ? globalThis.String(object.project) : "",
      specName: isSet(object.specName)
        ? globalThis.String(object.specName)
        : isSet(object.spec_name)
        ? globalThis.String(object.spec_name)
        : "",
      completedTasks: isSet(object.completedTasks)
        ? globalThis.Number(object.completedTasks)
        : isSet(object.completed_tasks)
        ? globalThis.Number(object.completed_tasks)
        : 0,
      totalTasks: isSet(object.totalTasks)
        ? globalThis.Number(object.totalTasks)
        : isSet(object.total_tasks)
        ? globalThis.Number(object.total_tasks)
        : 0,
    };
  },

  toJSON(message: SpecVelocityEntry): unknown {
    const obj: any = {};
    if (message.date !== "") {
      obj.date = message.date;
    }
    if (message.project !== "") {
      obj.project = message.project;
    }
    if (message.specName !== "") {
      obj.specName = message.specName;
    }
    if (message.completedTasks !== 0) {
      obj.completedTasks = Math.round(message.completedTasks);
    }
    if (message.totalTasks !== 0) {
      obj.totalTasks = Math.round(message.totalTasks);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SpecVelocityEntry>, I>>(base?: I): SpecVelocityEntry {
    return SpecVelocityEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SpecVelocityEntry>, I>>(object: I): SpecVelocityEntry {
    const message = createBaseSpecVelocityEntry();
    message.date = object.date ?? "";
    message.project = object.project ?? "";
    message.specName = object.specName ?? "";
    message.completedTasks = object.completedTasks ?? 0;
    message.totalTasks = object.totalTasks ?? 0;
    return message;
  },
};

function createBaseSpecVelocityResponse(): SpecVelocityResponse {
  return { entries: [] };
}

export const SpecVelocityResponse: MessageFns<SpecVelocityResponse> = {
  encode(message: SpecVelocityResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.entries) {
      SpecVelocityEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SpecVelocityResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSpecVelocityResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.entries.push(SpecVelocityEntry.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SpecVelocityResponse {
    return {
      entries: globalThis.Array.isArray(object?.entries)
        ? object.entries.map((e: any) => SpecVelocityEntry.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SpecVelocityResponse): unknown {
    const obj: any = {};
    if (message.entries?.length) {
      obj.entries = message.entries.map((e) => SpecVelocityEntry.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SpecVelocityResponse>, I>>(base?: I): SpecVelocityResponse {
    return SpecVelocityResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SpecVelocityResponse>, I>>(object: I): SpecVelocityResponse {
    const message = createBaseSpecVelocityResponse();
    message.entries = object.entries?.map((e) => SpecVelocityEntry.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
