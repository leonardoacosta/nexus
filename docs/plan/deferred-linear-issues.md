# Deferred Specs — Linear Filing Fallback

> No `.claude/linear-config.json` present — filing deferred to this markdown log instead of
> Linear per `/apply:all` Phase 6 Check 3.5 Branch A. Generated by `/apply:all` run
> `apply-2026-07-12-001`, wave 1.

## ios-session-navigation (1 unresolved task, 14/15 complete)

- **Task**: 3.3 — On-device verification requiring a physical Mac + iPhone with the macOS
  PtyViewer attached to a live session: type in the iOS attach screen, confirm keys reach the
  PTY and the macOS viewer flips to its read-only badge; capture the agent's
  `NXPTY interact open: writer-claim result` (`claimed:true` for the iOS open).
- **Beads**: task `nx-kwq1w`, feature `nx-6w2s0`, epic `nx-ywqig`.
- **Why deferred**: requires real hardware (Mac + iPhone simultaneously attached) that this
  automated run cannot drive. Confirm-skipped via the `/apply:all` Phase 0d user-gate preflight
  (2026-07-12) per Leo's explicit choice — no rationale text was requested for the skip.
- **Secondary blocker noted in the task text**: the notification-tap→pushed-view path is
  additionally verification-blocked on APNS provisioning (`nx-gsgvk`), independent of this task.
- **Spec state**: NOT archived — `openspec/changes/ios-session-navigation/` remains open with
  this one task unchecked. Re-run `/apply ios-session-navigation` (or do the on-device check by
  hand and tick the box) once hardware access is available.
