{
  "name": "@nexus/status",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "nexus-status": "src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile nexus-status",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.7.0"
  }
}
