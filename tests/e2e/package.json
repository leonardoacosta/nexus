{
  "name": "@nexus/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "End-to-end tests exercising the full stack (real Postgres, real agent HTTP/WS, real Next.js server action code paths).",
  "scripts": {
    "test": "bun run test:attach && bun run test:dashboard && bun run test:credentials-accounts",
    "test:attach": "bun test ./attach-websocket.test.ts",
    "test:dashboard": "cd ../../apps/nextjs && bun test ../../tests/e2e/dashboard-offline.test.ts",
    "test:credentials-accounts": "cd ../../apps/nextjs && bun test ../../tests/e2e/credentials-accounts.test.ts",
    "test:e2e": "bun run test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nexus/core": "workspace:*",
    "@nexus/db": "workspace:*",
    "@nexus/nextjs": "workspace:*",
    "@nexus/agent": "workspace:*",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.7.0"
  }
}
