# Tasks: settings-tab-redesign

<!-- beads:epic:nx-rxlbi -->
<!-- beads:feature:nx-2acxw -->

## API Batch

- [x] 1.1 Add `apps/swift/NexusShared/Config/AgentsConfigStore.swift` — `read() -> [AgentEntry]` (parse the existing TOML file), `write(_ entries: [AgentEntry]) throws` (atomic `.tmp + rename` write of the array-of-tables format), `validate(_ entry: AgentEntry) -> [ValidationError]` (URL + non-empty checks). Reuse the existing `AgentRegistry` parse logic; refactor into a shared parser so both Registry and Store consume the same code path. [beads:nx-af3aq]
- [x] 1.2 Extend `apps/swift/NexusShared/Networking/NexusAggregateClient.swift` with `rebootstrap()` — re-reads agents.toml via `AgentsConfigStore.read()` and rebuilds the per-agent `NexusClient` list. Cancels in-flight requests via `URLSession.invalidateAndCancel()` before swapping. **Conflict: shared file with 6 prior specs; wave-plan-build serializes.** [beads:nx-ymz1v]
- [x] 1.3 Add a NotificationCenter listener inside `NexusAggregateClient` for `AgentsConfigChanged`. On post, debounce 200ms then call `rebootstrap()`. Debounce ensures rapid edits don't churn requests. [beads:nx-na0yx]
- [x] 1.4 Add `apps/swift/NexusShared/Config/AgentsConfigStore.test.swift` covering: read round-trip, atomic write doesn't corrupt on mid-write crash (simulate by killing the .tmp before rename), validate rejects invalid endpoints, parse failure surfaces a typed error not a crash. [beads:nx-gd41u]

## UI Batch

- [x] 2.1 Rewrite `apps/swift/nexus-mac/Sources/Dashboard/SettingsView.swift` as a `NavigationSplitView` shell. Sidebar: 5 categories listed as `Label` with SF Symbol icons (`speaker.wave.2`, `bell.badge`, `network`, `slider.horizontal.3`, `waveform.path.ecg`). Detail pane: switch on selection enum to render one of the five new category views. Selection persists via `@AppStorage("settings.sidebar.selection")`. [beads:nx-5il4b]
- [x] 2.2 Add `apps/swift/nexus-mac/Sources/Dashboard/Settings/SettingsTtsView.swift`. Four blocks: TTS toggles (`tts_enabled`, `banner_enabled`, ducking picker, signal-only toggle — all backed by EXISTING persistence keys verbatim), ElevenLabs key panel (masked-show field + paste field + Test button + Save), global voice id text field backed by `Keychain.elevenLabsVoiceId`, and `ProjectVoicesView` mounted inline. [beads:nx-oiam3]
- [x] 2.3 Delete `apps/swift/nexus-mac/Sources/ElevenLabsSettingsView.swift` after confirming no external entry points reference it. Remove the file from `apps/swift/project.yml` if it's individually-listed (xcodegen-generated project will refresh on next `xcodegen generate`). [beads:nx-az24g]
- [x] 2.4 Add `apps/swift/nexus-mac/Sources/Dashboard/Settings/SettingsNotificationsView.swift` containing: banner-enabled toggle (cross-section override), default sort-mode picker (mirrors NotificationsView sort modes from notifications-overhaul), default group-by toggle, replay-autoplay toggle. All backed by `@AppStorage` keys prefixed `notifications.*`. [beads:nx-mu14j]
- [x] 2.5 Add `apps/swift/nexus-mac/Sources/Dashboard/Settings/SettingsAgentsView.swift` rendering an editable list driven by `AgentsConfigStore`. Per row: name + endpoint + machine text fields, inline validation captions, delete button. Footer: "Add agent" button + "interim seed" read-only row showing the legacy `dashboardEndpoint` (nx-4ohfs workaround). Save → AgentsConfigStore.write → post `AgentsConfigChanged`. [beads:nx-xbz6e]
- [x] 2.6 Implement the raw-TOML fallback editor inside SettingsAgentsView: when `AgentsConfigStore.read()` throws a parse error, render a `TextEditor` bound to the raw file contents with a Save Raw button. Save Raw writes the contents verbatim; the next view-render re-attempts the structured parse. [beads:nx-ugvr4]
- [x] 2.7 Add `apps/swift/nexus-mac/Sources/Dashboard/Settings/SettingsDashboardView.swift` containing: refresh interval Stepper (5-300s, backed by existing key), default view Picker (Sessions / Specs / Projects / Health / etc.), theme Picker (system / dark / light), accent color ColorPicker, font scale Picker (.normal / .compact). [beads:nx-c8n8j]
- [x] 2.8 Add `apps/swift/nexus-mac/Sources/Dashboard/Settings/SettingsDiagnosticsView.swift`. Loads `/health?detail=true` via existing aggregate client, formats a key/value list with traffic-light dots (green/yellow/red) keyed on staleness thresholds (30s/2min). Lists: last health snapshot ts+age, last watcher tick ts+age, socket-spine listening, db_ok, agents.toml entry count, dashboard build SHA, agent build SHA. [beads:nx-112m3]
- [x] 2.9 Implement the "Copy diagnostics to clipboard" button in SettingsDiagnosticsView. Build a plain-text payload under a `nexus diagnostics — <ISO-date>` header. Show a confirmation dialog (`.confirmationDialog`) with the exact payload and Copy / Cancel buttons. Only on Copy: write to `NSPasteboard.general.setString(payload, forType: .string)` and show a brief toast. [beads:nx-yrpys]
- [x] 2.10 Split `SettingsViewModel` into thin `SettingsRouterViewModel` (sidebar selection only) and per-category view-models (one per new view file). Existing persistence keys preserved verbatim; @AppStorage handles the lift. Shared logic (`AgentConnectionService`) extracted into NexusShared so SettingsAgentsView + SettingsDiagnosticsView consume the same network-test helper. [beads:nx-8ltyr]
- [x] 2.11 Add a launch-side migration check in `apps/swift/nexus/nexus/nexusApp.swift init()`: enumerate every expected `@AppStorage` / Keychain key, log warn-level via `appLogger.warning` when any are missing. NO mutation; observational only. List of expected keys derived from the per-category view-model field declarations. [beads:nx-s76uz]
- [x] 2.12 Add `apps/swift/nexus-mac/Sources/Dashboard/Settings/SettingsTtsViewTests.swift` covering: persistence key parity (set value pre-redesign, verify post-redesign reads it), key paste round-trip via Keychain mock, ProjectVoicesView integration smoke (mount + render). [beads:nx-85e22]
- [x] 2.13 Add `apps/swift/nexus-mac/Sources/Dashboard/Settings/SettingsAgentsViewTests.swift` covering: list rendering, add/edit/delete round-trip, invalid endpoint refused, AgentsConfigChanged notification posted on save, raw-TOML fallback activates on parse error. [beads:nx-anqxo]
- [x] 2.14 Add `apps/swift/nexus-mac/Sources/Dashboard/Settings/SettingsDiagnosticsViewTests.swift` covering: traffic-light thresholds, copy-with-confirmation flow (verify Cancel does NOT write to pasteboard), plain-text payload format. [beads:nx-dlazx]

## E2E Batch

- [ ] 3.1 End-to-end: launch dashboard with a pre-redesign UserDefaults state (specific values set for tts_enabled, refresh_interval, etc.), confirm all values render correctly in the redesigned views — no settings reset. **[DEFERRED — requires Mac dashboard UI run]** [beads:nx-ahzbz]
- [ ] 3.2 End-to-end: edit agents.toml via SettingsAgentsView (add a fake remote agent entry), confirm the file on disk has the new `[[agents]]` table with correct keys/values, confirm NexusAggregateClient rebootstraps within 2 seconds (observable via a probe call to GET /sessions returning fan-out from the new agent). **[DEFERRED — requires Mac dashboard UI run]** [beads:nx-df43v]
- [ ] 3.3 [user] Open Nexus.app Settings tab on Mac. Confirm: (a) sidebar shows 5 categories with icons; (b) selecting each category swaps the detail pane; (c) TTS & Audio shows the key paste field AND the per-project voices editor; (d) Agents pane lets you add a new agent and the dashboard picks it up; (e) Diagnostics pane shows liveness with traffic lights; (f) copy-diagnostics dialog requires explicit confirmation before writing to clipboard; (g) all toggles persist across relaunch. **[DEFERRED — user task per spec]** [beads:nx-ftk24]
- [x] 3.4 Confirm `ElevenLabsSettingsView.swift` is fully removed: `find apps/swift -name 'ElevenLabsSettingsView*'` returns empty. `git grep "ElevenLabsSettingsView"` returns zero matches outside the archive folder. [beads:nx-6kwvy]
