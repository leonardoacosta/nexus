<!-- beads:epic:nx-4oy2v -->
<!-- beads:feature:nx-qd1ko -->

# Implementation Tasks

## API Batch

- [ ] [2.1] [P-2] Migrate `packages/core/src/logger.ts` off `pino-opentelemetry-transport`: delete `buildTransport()` and the `transport` spread, keep `buildMixin()` unchanged. Drop the now-unused `pino-opentelemetry-transport` dependency once a repo-wide grep confirms no other caller. [owner:api-engineer] [type:config] [beads:nx-qpfsp]
- [ ] [2.2] [P-2] Add a `MeterProvider` + OTLP metrics exporter to `apps/agent/src/otel.ts`, sibling to the existing `NodeTracerProvider` (same endpoint-presence gate, same `@opentelemetry/sdk-metrics`/`@opentelemetry/exporter-metrics-otlp-http` vendor-package family — never `sdk-node`). Export `getMeter()`. Wire `apps/agent/src/health-collector.ts`'s existing 5s collection tick to record cpu/mem/disk/docker-container gauges via `getMeter()` — no new poller. Change the default `service.name` from `"nexus"` to `"nexus-agent"` in both providers. [owner:api-engineer] [type:config] [beads:nx-q1tkg]
- [ ] [2.3] [P-3] Wrap `createRequestHandler`'s dispatch in `apps/agent/src/server-request-handler.ts` with `getTracer().startActiveSpan(...)`, tagging `http.method`/`http.route`/`http.status_code`. Add an integration test asserting a dispatched request produces a span with those attributes (test exporter capture). [owner:api-engineer] [type:api] [beads:nx-5fe31]
- [ ] [2.4] [P-3] Add an `otel: {enabled, endpoint}` field to the `/version` response in `apps/agent/src/routes/version-builder.ts` (or the live capability/health surface), reading from the same env presence check `otel.ts` uses. Never include `OTEL_EXPORTER_OTLP_HEADERS`' value. [owner:api-engineer] [type:api] [beads:nx-02kyl]
- [ ] [2.5] [P-2] Add `OTEL_EXPORTER_OTLP_HEADERS` Basic-auth support to both the trace and metrics OTLP exporters in `otel.ts` — construct exporters explicitly with a `headers` option parsed by splitting on the FIRST `=` only (never the SDK's own env-var auto-parse, per the canon's documented `=`-padding footgun). [owner:api-engineer] [type:config] [beads:nx-6498l]
- [ ] [2.6] [P-3] Update `deploy/secrets.env.example`'s OTel section: document the on-host (`http://localhost:4318`, no auth) vs off-host (`https://otlp.leonardoacosta.dev` + `OTEL_EXPORTER_OTLP_HEADERS`) split, the `=`-padding footgun, and fix the file's stale header claim that the active secrets file is `~/.config/nexus/secrets.env` (actual path per `deploy/nexus-agent.service:66` is `~/.env`). [owner:docs-engineer] [type:docs] [beads:nx-n5alg]

## E2E Batch

- [ ] [4.1] [2.1] Unit test: `logger.ts` emits plain JSON to stdout with no pino transport attached whether or not `OTEL_EXPORTER_OTLP_ENDPOINT` is set, and the mixin still injects `traceId`/`spanId` when a span is active. [owner:tdd-integration] [type:testing] [beads:nx-jfxxo]
- [ ] [4.2] [2.2] Unit test: `getMeter()` records a gauge that an injectable test exporter can observe (mirror the `HealthCollectorDeps` injectable-seam pattern — no Bun process-global `mock.module`). Cover both the endpoint-set and endpoint-unset branches. [owner:tdd-integration] [type:testing] [beads:nx-wgwzh]
- [ ] [4.3] Manual smoke (not automated — see design.md § Verification plan): run nexus-agent locally against a throwaway `otel/opentelemetry-collector` with a `debug` exporter; confirm BOTH trace and metric records arrive (not just traces — mirrors the documented CC-side metrics-silently-no-op failure mode this must not repeat). Confirm `bun build --compile` still succeeds after adding the metrics packages. [owner:api-engineer] [type:testing] [beads:nx-oatox]
- [ ] [4.4] [user] Verify OTLP data actually lands on `grafana.leonardoacosta.dev` after this ships and `OTEL_EXPORTER_OTLP_ENDPOINT` is enabled on a live machine's `~/.env` — searched: `deploy/`, `homelab/scripts` for an automated dashboard-verify helper; none exists, and confirming live Grafana panel/trace data requires viewing the UI directly. [type:config] [beads:nx-m2xi7]
- [ ] [4.5] [user] Enable `OTEL_EXPORTER_OTLP_ENDPOINT` (+ `OTEL_SERVICE_NAME=nexus-agent`, + `OTEL_EXPORTER_OTLP_HEADERS` on any non-homelab machine) in each machine's live `~/.env` — searched: `deploy/install.sh`, `deploy/secrets.env.example`; no automated cross-machine secrets-push mechanism exists by design (each `~/.env` is machine-local and gitignored). Test the Basic-auth header value with a direct `curl` against `otlp.leonardoacosta.dev` before trusting it in `.env`, per the canon's footgun note. [type:config] [beads:nx-tbbz9]
