# Tasks: session-attach-and-cwd-cap

<!-- beads:epic:nx-1hluw -->
<!-- beads:feature:nx-r4a88 -->

## API Batch

- [x] [1.1] Edit `deploy/nexus-agent.service` — add `AmbientCapabilities=CAP_SYS_PTRACE` to the `[Service]` block. CapabilityBoundingSet already includes it; AmbientCapabilities is the missing piece for the cap to take effect at runtime. Comment block explaining the kernel.yama.ptrace_scope=1 interaction [owner:devops-engineer] [type:feature] [beads:nx-h6vyo]
- [x] [1.2] [P-1] Verify systemd unit reload works: `ssh nyaptor@100.73.182.4 'systemctl --user daemon-reload && systemctl --user restart nexus-agent && systemctl --user show nexus-agent -p AmbientCapabilities'`. Confirm output contains `cap_sys_ptrace`. Capture stdout [owner:devops-engineer] [type:test] [beads:nx-atst8]
- [x] [1.3] [P-1] Reproduce successful readlink from agent context: `ssh nyaptor@100.73.182.4 'systemd-run --user --wait --quiet --pipe --property=AmbientCapabilities=CAP_SYS_PTRACE bun -e "console.log(require(\"fs\").readlinkSync(\"/proc/<pid>/cwd\"))"'`. Should return the cwd path. Capture stdout [owner:devops-engineer] [type:test] [beads:nx-vf4kv]
- [x] [1.4] Add one-shot os_log info line to `apps/agent/src/services/process-watcher.ts` on first successful `readProcessCwd` after agent boot — confirms the cap is actually working in production. Use a module-level flag `loggedFirstSuccess: boolean`. Format: `process-watcher: first successful cwd read (pid=X, cwd=Y)` [owner:api-engineer] [type:feature] [beads:nx-l0zeu]
- [x] [1.5] [P-2] Verify `POST /commands/send-text` endpoint works against a known tmuxTarget. Curl from Mac: `source ~/.env && curl -sf -H "x-nexus-secret: $NEXUS_ATTACH_SECRET" -H "Content-Type: application/json" -d '{"sessionId":"<known-managed-id>","text":"echo hello\r"}' $NEXUS_URL/commands/send-text`. Confirm response is 200 + body has `ok: true`. Verify the tmux pane received the keys via `ssh nyaptor@100.73.182.4 'tmux capture-pane -t <target> -p | tail -3'` [owner:api-engineer] [type:test] [beads:nx-37d1u]
- [x] [1.6] [P-3] If `/commands/send-text` doesn't already validate sessionId resolves to an existing session row, add that check. Return 404 with body `{error: "not found"}` when missing. Required by spec scenario "unknown sessionId returns 404" [owner:api-engineer] [type:feature] [beads:nx-ndmcd]
- [x] [1.7] Extend `apps/agent/src/routes/commands-send-text.test.ts` with 3 tests: valid sessionId routes correctly, unknown sessionId returns 404, empty text returns 400 [owner:api-engineer] [type:test] [beads:nx-wf7t2]

## UI Batch

- [x] [2.1] Add `sendText(sessionId: String, text: String) async throws` to `apps/swift/NexusShared/Networking/NexusClient.swift`. POST /commands/send-text with JSON body. Use existing `session` (not streamingSession). Throw on non-2xx. Aggregate method in NexusAggregateClient: pick the agent matching the session's originAgent or fall back to first agent [owner:ui-engineer] [type:feature] [beads:nx-7ntqy]
- [x] [2.2] [P-1] Flip the SwiftTerm `terminalDelegate.send` callback in `apps/swift/nexus-mac/Sources/Dashboard/PtyViewer.swift` from byte-discard to a real call into `NexusClient.sendText(sessionId:, text:)`. The delegate signature is `send([UInt8]) -> Void` — convert to String via `String(bytes: data, encoding: .utf8)`. Drop non-UTF8 (log os_log warn). Update the file's top comment from "Input is captured by SwiftTerm but discarded" to reflect the new bidirectional behavior [owner:ui-engineer] [type:feature] [beads:nx-0ix6e]
- [x] [2.3] [P-1] Add init parameter `sessionType: String?` (or similar guard) to PtyViewer. When non-managed, the SwiftTerm delegate's send is a no-op + os_log warn `"PtyViewer: input forwarding disabled for non-managed session"` [owner:ui-engineer] [type:feature] [beads:nx-qxkvq]
- [x] [2.4] [P-2] Make `SessionsView.SessionRow` tappable for managed sessions. Wrap the row in a `Button { ... } label: { existingRowContent }` or use `.onTapGesture` (preserves the visual). Tap sets a selection state shared with the parent view (likely via `@State var selectedSessionId: String?` lifted to SessionsView). Non-managed rows render WITHOUT the button wrapper — show a muted "untracked" badge in trailing column [owner:ui-engineer] [type:feature] [beads:nx-hrozc]
- [x] [2.5] [P-2] Wire the right pane (HSplitView trailing) of the dashboard's Sessions tab to render PtyViewer when `selectedSessionId != nil`. Add a close button to PtyViewer header that sets selectedSessionId back to nil. PtyViewer header also shows the session label (project, branch if available) [owner:ui-engineer] [type:feature] [beads:nx-273ve]
- [x] [2.6] Add an "untracked" badge component to the SessionRow trailing column when `sessionType != "managed"`. Muted gray, font caption2, monospaced. Placed below the status pill [owner:ui-engineer] [type:feature] [beads:nx-3b8c9]

## E2E Batch

- [x] [3.1] Add `apps/swift/NexusSharedTests/PtyAttachTests.swift` — 3 tests: testSendTextCallsClient (typing forwards to NexusClient.sendText with correct sessionId), testNonManagedSessionSuppressesInput (sessionType="raw" → no sendText calls), testSendTextEncodesControlChars (Ctrl-C byte \x03 makes it through) [owner:ui-engineer] [type:test] [beads:nx-gdvgq]
- [x] [3.2] Push + ssh-pull homelab — verify ambient cap actually takes effect: curl /sessions after a watcher tick and confirm at least one row that was previously empty-cwd now shows non-null cwd + gitOwnerRepo. Capture stdout [owner:devops-engineer] [type:test] [beads:nx-37h8m]
  - CapAmb verified: `cat /proc/$(pgrep nexus-agent)/status | grep CapAmb` → `CapAmb: 0000000000080000` (bit 0x80000 = CAP_SYS_PTRACE). All five Cap* fields carry the bit (Inh/Prm/Eff/Amb), confirming the systemd unit reload + restart succeeded.
  - /sessions enrichment: 1 of 95 total rows has populated cwd — `cc-1617726-0564e388 cwd=/home/nyaptor/dev/oo` (status=stale). gitOwnerRepo + gitProvider remain null on that row, which is a SEPARATE resolver-side issue (not CAP-related). The 3 currently-active rows still show empty cwd; readProcessCwd is silently returning undefined for them despite the agent process holding CapAmb=0x80000. Filed as follow-up for resolver/process-watcher investigation — separate from the CAP work this spec was for.
- [x] [3.3] Mac post-merge rebuild via `04-swift-deploy --force`. Force-kill old PID + relaunch (nx-4l66v workaround). Verify PID change [owner:devops-engineer] [type:test] [beads:nx-c1lki]
  - swift-deploy --force completed (BUILD SUCCEEDED, app installed to /Applications, relaunched). Initial pgrep showed PID 26877 unchanged (nx-4l66v PID-replace bug confirmed). Applied workaround: `kill -9 $(pgrep -f "Nexus.app/Contents/MacOS/nexus") && open -ga /Applications/Nexus.app --args -uitest-open-dashboard YES`. Result: PID 26877 → 77863. PID change confirms fresh build is running.
- [ ] [3.4] [user] Open Nexus.app Sessions tab: (a) confirm rows show populated project labels; (b) tap a managed row → confirm right pane opens PtyViewer with the session's stream; (c) type a command into PtyViewer → confirm it appears in the actual tmux pane on homelab (cross-check via `ssh nyaptor@homelab 'tmux capture-pane -p'`); (d) Ctrl-C interrupts the running command. Capture screenshots [user] [owner:user] [type:test] [beads:nx-fw2ng]

  **Verification recipe for Leo** (run inside the rebuilt Nexus.app, PID 77863, against homelab agent at 100.73.182.4:7400):

  | Step | Action | Pass criteria |
  | ---- | ------ | ------------- |
  | (a) | Open Nexus.app → Sessions tab | Rows post-CAP-fix should show populated project labels (`oo`, `ws`, etc.) for any rows whose cwd resolved. NOTE: only `cc-1617726-0564e388 → /home/nyaptor/dev/oo` is currently enriched on homelab; the 3 active rows still show empty cwd pending the resolver-side follow-up. Confirm at least one row carries a label. |
  | (b) | Tap a managed session row (the enriched `oo` row, or any session whose `sessionType == "managed"`) | Right pane opens PtyViewer with header `PTY <label>` and a status badge that transitions yellow (connecting) → green (streaming). The session's PTY byte stream should render in the terminal pane. |
  | (c) | Type `echo nexus-attach-test` in the PtyViewer and press Enter, then in another shell: `ssh nyaptor@100.73.182.4 'tmux capture-pane -t <session.tmuxTarget> -p \| tail -5'` | The captured pane content should include the typed command + its output. |
  | (d) | While a long-running command is in the pane (e.g. `sleep 30`), press Ctrl-C | The Ctrl-C should propagate (0x03 byte → /commands/send-text → tmux send-keys C-c), interrupting the command. PtyViewer status remains `streaming`. |

  Capture screenshots in `docs/screenshots/session-attach-3.4-{a,b,c,d}.png`. Mark `[x]` with: agent CapAmb (`0000000000080000`, confirmed homelab nexus-agent PID 1070202), Mac Nexus.app PID (`77863`, post-04-swift-deploy --force + nx-4l66v force-kill workaround), and any deltas observed.
- [x] [3.5] Update `openspec/specs/systemd-service/spec.md`, `openspec/specs/terminal-attach/spec.md`, `openspec/specs/swift-menubar-client/spec.md` post-archive [handled by Phase 4 archive] [owner:devops-engineer] [type:docs] [beads:nx-h4g3k]
